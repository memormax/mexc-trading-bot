# 📋 Руководство по работе с логами бота на сервере

## 🎯 Быстрый доступ к логам

### 1. **Просмотр логов в реальном времени (рекомендуется)**
```bash
pm2 logs mexc-trading-bot
```
- Показывает логи в реальном времени (как `tail -f`)
- Показывает и ошибки, и обычные логи вместе
- Нажмите `Ctrl+C` для выхода

### 2. **Последние N строк логов**
```bash
pm2 logs mexc-trading-bot --lines 100
```
- Показывает последние 100 строк
- Можно указать любое число (50, 200, 500)

### 3. **Только ошибки**
```bash
pm2 logs mexc-trading-bot --err
```

### 4. **Только обычные логи (без ошибок)**
```bash
pm2 logs mexc-trading-bot --out
```

---

## 📁 Файлы логов

Логи сохраняются в файлы в директории `~/unified-bot/logs/`:

- **`pm2-error.log`** - все ошибки
- **`pm2-out.log`** - все обычные логи (включая INFO, DEBUG)

### Просмотр файлов напрямую

```bash
# Перейти в директорию с логами
cd ~/unified-bot/logs

# Посмотреть последние 100 строк ошибок
tail -n 100 pm2-error.log

# Посмотреть последние 100 строк обычных логов
tail -n 100 pm2-out.log

# Просмотр в реальном времени
tail -f pm2-error.log
tail -f pm2-out.log

# Поиск по логам (например, найти все упоминания "комиссия")
grep -i "комиссия" pm2-out.log
grep -i "комиссия" pm2-error.log

# Поиск по дате (например, логи за сегодня)
grep "2025-12-28" pm2-out.log
```

---

## 🔍 Диагностика проблем

### Что случилось с ботом?

#### 1. **Проверьте статус бота**
```bash
pm2 status
```
- `online` - бот работает
- `stopped` - бот остановлен
- `errored` - бот упал с ошибкой
- `restarting` - бот перезапускается

#### 2. **Проверьте последние ошибки**
```bash
pm2 logs mexc-trading-bot --err --lines 50
```

#### 3. **Проверьте что происходило перед остановкой**
```bash
# Последние 200 строк (чтобы увидеть контекст)
pm2 logs mexc-trading-bot --lines 200
```

#### 4. **Поиск конкретных событий**

```bash
# Найти все переключения аккаунтов
grep -i "переключ" ~/unified-bot/logs/pm2-out.log

# Найти все ошибки открытия позиций
grep -i "ошибка открытия" ~/unified-bot/logs/pm2-error.log

# Найти все упоминания комиссии
grep -i "комиссия" ~/unified-bot/logs/pm2-out.log

# Найти все упоминания конкретного аккаунта
grep -i "аккаунт.*3" ~/unified-bot/logs/pm2-out.log

# Найти все критические ошибки
grep -i "критич" ~/unified-bot/logs/pm2-error.log
```

---

## 📊 Мониторинг в реальном времени

### 1. **PM2 Monitor (визуальный монитор)**
```bash
pm2 monit
```
- Показывает CPU, память, логи в реальном времени
- Нажмите `Ctrl+C` для выхода

### 2. **Логи с фильтрацией**
```bash
# Только логи мультиаккаунтинга
pm2 logs mexc-trading-bot | grep -i "multi-account"

# Только логи торговли
pm2 logs mexc-trading-bot | grep -i "trade\|signal"

# Только ошибки API
pm2 logs mexc-trading-bot | grep -i "ошибка.*api"
```

---

## 🕐 Поиск по времени

### Логи за конкретный период

```bash
# Логи за сегодня (если формат даты YYYY-MM-DD)
grep "$(date +%Y-%m-%d)" ~/unified-bot/logs/pm2-out.log

# Логи за конкретную дату
grep "2025-12-28" ~/unified-bot/logs/pm2-out.log

# Логи за последний час (если формат времени HH:MM)
grep "$(date +%H)" ~/unified-bot/logs/pm2-out.log
```

---

## 🧹 Управление логами

### Очистка логов PM2
```bash
# Очистить все логи PM2 (не удаляет файлы, только буфер)
pm2 flush

# Удалить файлы логов (если они слишком большие)
rm ~/unified-bot/logs/pm2-*.log
# PM2 создаст новые файлы автоматически
```

### Проверка размера логов
```bash
# Размер всех логов PM2
du -sh ~/.pm2/logs/

# Размер логов бота
du -sh ~/unified-bot/logs/
```

---

## 🎯 Типичные сценарии диагностики

### Сценарий 1: "Бот остановился, не понимаю почему"

```bash
# 1. Проверьте статус
pm2 status

# 2. Посмотрите последние 200 строк логов
pm2 logs mexc-trading-bot --lines 200

# 3. Найдите последнее переключение аккаунта
grep -i "переключ\|stop\|останов" ~/unified-bot/logs/pm2-out.log | tail -20

# 4. Проверьте ошибки
pm2 logs mexc-trading-bot --err --lines 50
```

### Сценарий 2: "Аккаунт ушел в минус, что произошло?"

```bash
# 1. Найдите все сделки на этом аккаунте
grep -i "аккаунт.*3" ~/unified-bot/logs/pm2-out.log | tail -50

# 2. Найдите все открытия/закрытия позиций
grep -i "открыт\|закрыт\|position" ~/unified-bot/logs/pm2-out.log | tail -50

# 3. Проверьте баланс аккаунта в логах
grep -i "баланс" ~/unified-bot/logs/pm2-out.log | grep -i "аккаунт.*3" | tail -20

# 4. Проверьте ошибки во время торговли
grep -i "ошибка" ~/unified-bot/logs/pm2-error.log | tail -30
```

### Сценарий 3: "Бот не переключается на следующий аккаунт"

```bash
# 1. Проверьте статусы аккаунтов
grep -i "статус\|status" ~/unified-bot/logs/pm2-out.log | tail -30

# 2. Проверьте условия переключения
grep -i "переключ\|switch\|minBalance\|targetBalance" ~/unified-bot/logs/pm2-out.log | tail -30

# 3. Проверьте ошибки переключения
grep -i "ошибка.*переключ\|error.*switch" ~/unified-bot/logs/pm2-error.log
```

### Сценарий 4: "Обнаружена комиссия, что дальше?"

```bash
# 1. Найдите момент обнаружения комиссии
grep -i "комиссия.*обнаружена\|commission.*detected" ~/unified-bot/logs/pm2-out.log | tail -20

# 2. Проверьте что произошло после обнаружения
# (после строки с комиссией посмотрите следующие 50 строк)
grep -A 50 -i "комиссия.*обнаружена" ~/unified-bot/logs/pm2-out.log | tail -50

# 3. Проверьте переключение аккаунта
grep -i "переключ.*комиссия\|switch.*commission" ~/unified-bot/logs/pm2-out.log
```

---

## 📝 Полезные команды для анализа

### Экспорт логов в файл
```bash
# Экспортировать последние 1000 строк в файл
pm2 logs mexc-trading-bot --lines 1000 > ~/bot-logs-export.txt

# Экспортировать только ошибки
pm2 logs mexc-trading-bot --err --lines 500 > ~/bot-errors-export.txt
```

### Поиск паттернов
```bash
# Все упоминания конкретного ордера (замените ORDER_ID)
grep "ORDER_ID" ~/unified-bot/logs/pm2-out.log

# Все упоминания конкретной позиции
grep "positionId.*1211103411" ~/unified-bot/logs/pm2-out.log

# Все API запросы к бирже
grep -i "api.*request\|submitting order" ~/unified-bot/logs/pm2-out.log
```

---

## ⚠️ Важные моменты

1. **Логи не бесконечные**: PM2 может ограничивать размер логов. Если логи старые, они могут быть удалены.

2. **Ротация логов**: PM2 автоматически ротирует логи, но старые логи могут быть удалены. Регулярно экспортируйте важные логи.

3. **Формат времени**: Логи содержат временные метки в формате ISO (например, `2025-12-28T06:10:44.459Z`).

4. **Кодировка**: Логи могут содержать русские символы. Убедитесь, что ваш терминал поддерживает UTF-8.

---

## 🚀 Быстрая справка

```bash
# Статус бота
pm2 status

# Логи в реальном времени
pm2 logs mexc-trading-bot

# Последние 100 строк
pm2 logs mexc-trading-bot --lines 100

# Только ошибки
pm2 logs mexc-trading-bot --err

# Монитор
pm2 monit

# Очистить логи
pm2 flush
```

---

## 📞 Если нужна помощь

При обращении за помощью приложите:
1. Статус бота: `pm2 status`
2. Последние 200 строк логов: `pm2 logs mexc-trading-bot --lines 200`
3. Последние 50 ошибок: `pm2 logs mexc-trading-bot --err --lines 50`
4. Контекст проблемы (что вы делали, когда произошла проблема)




