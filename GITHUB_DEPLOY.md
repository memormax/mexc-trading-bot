# 📤 Инструкция по загрузке на GitHub и деплою на сервер

**Репозиторий:** https://github.com/memormax/mexc-trading-bot  
**Версия:** 2.1.0  
**Дата:** 2026-02-05

## 📋 Подготовка к загрузке

### 1. Проверка текущего состояния

```bash
cd unified-bot

# Проверить статус Git
git status

# Проверить удаленный репозиторий
git remote -v
```

Если репозиторий не настроен:
```bash
git remote add origin https://github.com/memormax/mexc-trading-bot.git
```

## 🚀 Загрузка на GitHub

### Шаг 1: Добавление изменений

```bash
# Добавить все изменения (кроме игнорируемых файлов)
git add .

# Или добавить конкретные файлы
git add server.ts
git add services/
git add package.json
git add README.md
git add DEPLOYMENT_READY.md
git add DEPLOYMENT_CHECKLIST.md
```

### Шаг 2: Проверка что будет загружено

```bash
# Посмотреть что будет в коммите
git status

# Посмотреть изменения
git diff --cached
```

### Шаг 3: Создание коммита

```bash
# Создать коммит с описанием изменений
git commit -m "v2.1.0: Добавлен импорт аккаунтов, сброс статуса, таймаут между сделками, исправления блокировки и отображения ошибок"
```

Или более подробное описание:
```bash
git commit -m "v2.1.0: Обновление Flipbot

- Добавлен импорт аккаунтов из txt файла (как в Ferm)
- Добавлена кнопка сброса статуса аккаунта
- Добавлен таймаут между сделками
- Исправлена проблема с блокировкой бота в админке
- Исправлена проблема с отображением причины остановки аккаунта
- Улучшена обработка ошибок биржи
- Обновлена документация"
```

### Шаг 4: Загрузка на GitHub

```bash
# Загрузить изменения на GitHub (в ветку main)
git push origin main

# Или если используете другую ветку (например, master)
git push origin master
```

Если это первый push или нужно установить upstream:
```bash
git push -u origin main
```

## 🔄 Если возникли конфликты

Если на GitHub есть изменения, которых нет локально:

```bash
# Получить изменения с GitHub
git pull origin main

# Разрешить конфликты (если есть)
# Затем снова загрузить
git push origin main
```

## 🖥️ Деплой на сервер

### Вариант 1: Через Git Pull (рекомендуется)

```bash
# Подключиться к серверу
ssh user@your-server.com

# Перейти в директорию проекта
cd /path/to/unified-bot

# Получить последние изменения с GitHub
git pull origin main

# Установить зависимости (если нужно)
npm install --production

# Пересобрать проект
npm run build

# Перезапустить через PM2
pm2 restart mexc-trading-bot

# Или если используете другой способ запуска
pm2 stop mexc-trading-bot
pm2 start ecosystem.config.js
pm2 save
```

### Вариант 2: Через скрипт деплоя

```bash
# На сервере
cd /path/to/unified-bot
git pull origin main
./deploy.sh
```

### Вариант 3: Полная переустановка (если нужно)

```bash
# На сервере
cd /path/to
rm -rf unified-bot
git clone https://github.com/memormax/mexc-trading-bot.git unified-bot
cd unified-bot
npm install --production
npm run build
pm2 start ecosystem.config.js
pm2 save
```

## ✅ Проверка после деплоя

```bash
# Проверка статуса PM2
pm2 status

# Просмотр логов
pm2 logs mexc-trading-bot

# Проверка доступности
curl http://localhost:3002/api/status

# Проверка версии
curl http://localhost:3002/api/status | grep version
```

## 📝 Полная последовательность команд

### На локальной машине (Windows):

```powershell
cd D:\Cursors\uid\unified-bot

# Проверить статус
git status

# Добавить изменения
git add .

# Создать коммит
git commit -m "v2.1.0: Обновление Flipbot - импорт аккаунтов, сброс статуса, таймаут между сделками"

# Загрузить на GitHub
git push origin main
```

### На сервере (Linux):

```bash
cd /path/to/unified-bot

# Получить изменения
git pull origin main

# Пересобрать
npm run build

# Перезапустить
pm2 restart mexc-trading-bot

# Проверить
pm2 status
pm2 logs mexc-trading-bot --lines 50
```

## 🔐 Безопасность

### Рекомендации:

1. **Не коммитьте секретные данные:**
   - Убедитесь, что `.gitignore` правильно настроен
   - Не коммитьте файлы с API ключами, паролями
   - Используйте переменные окружения для секретов

2. **Проверьте перед коммитом:**
   ```bash
   git status
   git diff
   ```

3. **Используйте теги для версий:**
   ```bash
   git tag -a v2.1.0 -m "Версия 2.1.0"
   git push origin v2.1.0
   ```

## 🐛 Решение проблем

### Проблема: "fatal: not a git repository"
```bash
git init
git remote add origin https://github.com/memormax/mexc-trading-bot.git
```

### Проблема: "Permission denied"
```bash
# Проверить права доступа к репозиторию
# Убедиться, что у вас есть права на запись
```

### Проблема: "Updates were rejected"
```bash
# Получить изменения с GitHub
git pull origin main --rebase

# Затем снова загрузить
git push origin main
```

### Проблема: Конфликты при pull
```bash
# Посмотреть конфликты
git status

# Разрешить конфликты вручную
# Затем:
git add .
git commit -m "Разрешение конфликтов"
git push origin main
```

## 📦 Что НЕ нужно коммитить

Следующие файлы/директории игнорируются (указаны в `.gitignore`):
- `node_modules/` - зависимости
- `dist/` - скомпилированные файлы
- `data/` - данные пользователей
- `logs/` - логи
- `backup/` - бекапы
- `.env` - переменные окружения

## 🎯 Быстрая команда для деплоя

Создайте скрипт `deploy-server.sh` на сервере:

```bash
#!/bin/bash
cd /path/to/unified-bot
git pull origin main
npm install --production
npm run build
pm2 restart mexc-trading-bot
echo "✅ Деплой завершен!"
```

Сделайте его исполняемым:
```bash
chmod +x deploy-server.sh
```

Использование:
```bash
./deploy-server.sh
```

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи: `pm2 logs mexc-trading-bot`
2. Проверьте статус: `pm2 status`
3. Проверьте Git: `git status` и `git log`
4. Проверьте доступность GitHub: `curl https://github.com/memormax/mexc-trading-bot`
