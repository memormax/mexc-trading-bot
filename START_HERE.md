# 🚀 НАЧНИТЕ ОТСЮДА: Загрузка на GitHub

## ⚠️ ВАЖНО: Вы должны быть в правильной директории!

Проект находится здесь: **`D:\Cursors\uid\unified-bot`**

## 📍 Шаг 1: Перейдите в директорию проекта

В PowerShell выполните:

```powershell
cd D:\Cursors\uid\unified-bot
```

**Проверьте, что вы в правильной директории:**
```powershell
pwd
# Должно показать: D:\Cursors\uid\unified-bot
```

## 📤 Шаг 2: Загрузка на GitHub

После того как вы перешли в правильную директорию, выполните:

```powershell
# 1. Проверить статус
git status

# 2. Добавить все изменения
git add .

# 3. Создать коммит
git commit -m "v2.1.0: Импорт аккаунтов, сброс статуса, таймаут между сделками, исправления блокировки"

# 4. Загрузить на GitHub
git push origin main
```

## ✅ Готово!

После выполнения этих команд все изменения будут загружены на GitHub.

## 🖥️ Затем на сервере:

```bash
cd /path/to/unified-bot
git pull origin main
npm run build
pm2 restart mexc-trading-bot
```
