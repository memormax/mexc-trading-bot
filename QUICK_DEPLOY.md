# ⚡ Быстрый деплой (шпаргалка)

**Версия:** 1.0.1  
**Дата обновления:** 2026-01-02

---

## 📤 1. Загрузка на GitHub (локально, в PowerShell)

```powershell
cd D:\Cursors\uid\unified-bot

# Проверка статуса
git status

# Добавить все изменения
git add .

# Коммит с описанием
git commit -m "Версия 1.0.1: улучшенная безопасность, маскирование API ключей, исправления"

# Отправить на GitHub
git push origin main
```

**Важно:** Убедитесь, что:
- ✅ Все изменения сохранены
- ✅ Проект скомпилирован (`npm run build`)
- ✅ Нет ошибок в коде

---

## 🚀 2. Деплой на сервер (SSH подключение)

### Шаг 1: Подключение к серверу
```bash
ssh root@45.32.53.81
```

### Шаг 2: Остановка бота
```bash
pm2 stop mexc-trading-bot
```

### Шаг 3: Бекап текущей версии
```bash
# Создать бекап текущей версии
cp -r /root/unified-bot /root/unified-bot_backup_$(date +%Y%m%d_%H%M%S)

# Проверить, что бекап создан
ls -la /root/unified-bot_backup_*
```

### Шаг 4: Обновление кода из GitHub
```bash
cd /root/unified-bot

# Получить последние изменения
git pull origin main

# Если есть конфликты, решить их вручную
# git status  # проверить статус
```

### Шаг 5: Установка зависимостей
```bash
# Установить новые зависимости (если есть)
npm install --production
```

### Шаг 6: Сборка проекта
```bash
# Скомпилировать TypeScript
npm run build

# Проверить, что сборка прошла успешно
ls -la dist/server.js
```

### Шаг 7: Создание необходимых директорий
```bash
# Создать директории для данных и логов (если их нет)
mkdir -p data
mkdir -p logs
mkdir -p backup
```

### Шаг 8: Перезапуск бота
```bash
# Перезапустить через PM2
pm2 restart mexc-trading-bot

# Или если бот не запущен
pm2 start ecosystem.config.js
```

### Шаг 9: Проверка статуса
```bash
# Проверить статус
pm2 status

# Посмотреть логи
pm2 logs mexc-trading-bot --lines 50
```

---

## ✅ 3. Проверка после деплоя

### Проверка веб-интерфейса
- Откройте: http://45.32.53.81:3002/
- Проверьте главную страницу
- Проверьте страницу отчетов: http://45.32.53.81:3002/reports.html

### Проверка логов
```bash
# Логи в реальном времени
pm2 logs mexc-trading-bot

# Последние 100 строк
pm2 logs mexc-trading-bot --lines 100

# Только ошибки
pm2 logs mexc-trading-bot --err --lines 50
```

### Проверка статуса
```bash
pm2 status
pm2 info mexc-trading-bot
```

### Проверка работы функций
- ✅ Мультиаккаунтинг работает
- ✅ Отчеты сохраняются в `data/account-reports.json`
- ✅ API ключи маскируются в отчетах
- ✅ Мини-приложение для балансов работает

---

## 📋 4. Работа с логами

### Быстрый просмотр
```bash
# Логи в реальном времени
pm2 logs mexc-trading-bot

# Последние 100 строк
pm2 logs mexc-trading-bot --lines 100

# Только ошибки
pm2 logs mexc-trading-bot --err --lines 50
```

### Диагностика проблем
```bash
# Что произошло перед остановкой?
pm2 logs mexc-trading-bot --lines 200

# Поиск по логам
grep -i "комиссия\|ошибка\|переключ" ~/unified-bot/logs/pm2-out.log | tail -30

# Поиск ошибок
grep -i "error\|exception\|failed" ~/unified-bot/logs/pm2-error.log | tail -50
```

**📖 Подробное руководство:** см. `LOGS_GUIDE.md`

---

## 🔄 5. Откат (если что-то пошло не так)

### Быстрый откат
```bash
# Найти бекап
ls -la /root/unified-bot_backup_*

# Остановить бота
pm2 stop mexc-trading-bot

# Восстановить из бекапа (замените дату)
rm -rf /root/unified-bot
cp -r /root/unified-bot_backup_YYYYMMDD_HHMMSS /root/unified-bot

# Перейти в директорию
cd /root/unified-bot

# Установить зависимости
npm install --production

# Скомпилировать
npm run build

# Перезапустить
pm2 restart mexc-trading-bot

# Проверить
pm2 status
pm2 logs mexc-trading-bot --lines 50
```

---

## 🛠️ 6. Автоматический деплой (альтернатива)

Если на сервере есть скрипт `deploy.sh`:

```bash
cd /root/unified-bot
git pull origin main
./deploy.sh
```

Скрипт автоматически:
- ✅ Проверит Node.js и npm
- ✅ Установит зависимости
- ✅ Скомпилирует проект
- ✅ Перезапустит через PM2

---

## 📝 7. Чеклист перед деплоем

Перед загрузкой на GitHub убедитесь:

- [ ] Все изменения протестированы локально
- [ ] Проект компилируется без ошибок (`npm run build`)
- [ ] Нет ошибок TypeScript
- [ ] Обновлена версия в `CURRENT_VERSION.md`
- [ ] Создан бекап текущей версии (локально)
- [ ] Проверен `.gitignore` (не загружаются лишние файлы)
- [ ] Проверено, что `data/` в `.gitignore` (отчеты не загружаются)

---

## 🔐 8. Важные замечания

### Безопасность
- ✅ API ключи маскируются в отчетах (первые 4 + ... + последние 4 символа)
- ✅ Кнопка "Копировать" удалена из страницы отчетов
- ✅ Файл `data/account-reports.json` не загружается в GitHub (в `.gitignore`)

### Данные
- Отчеты сохраняются в `data/account-reports.json` на сервере
- При обновлении кода отчеты **не удаляются** (файл в `.gitignore`)
- Для очистки отчетов используйте кнопку "Очистить" на странице отчетов

### Мини-приложение
- Полностью изолировано от основного бота
- Можно использовать во время работы основного бота
- Не влияет на торговлю

---

## 📞 9. Полезные команды

```bash
# Перезапуск бота
pm2 restart mexc-trading-bot

# Остановка бота
pm2 stop mexc-trading-bot

# Запуск бота
pm2 start mexc-trading-bot

# Удаление из PM2
pm2 delete mexc-trading-bot

# Сохранение конфигурации PM2
pm2 save

# Просмотр использования ресурсов
pm2 monit

# Очистка логов
pm2 flush
```

---

## 🆘 10. Решение проблем

### Бот не запускается
```bash
# Проверить логи
pm2 logs mexc-trading-bot --err

# Проверить, что файл собран
ls -la dist/server.js

# Пересобрать проект
npm run build
```

### Ошибки при сборке
```bash
# Очистить и пересобрать
rm -rf dist node_modules
npm install
npm run build
```

### Проблемы с портом
```bash
# Проверить, занят ли порт
netstat -tulpn | grep 3002

# Изменить порт в ecosystem.config.js
# Или остановить процесс, занимающий порт
```

---

**📖 Дополнительная документация:**
- `DEPLOYMENT_GUIDE.md` - Подробное руководство по развертыванию
- `DEPLOY_CHECKLIST.md` - Чеклист перед развертыванием
- `CURRENT_VERSION.md` - Информация о текущей версии
- `LOGS_GUIDE.md` - Руководство по работе с логами
