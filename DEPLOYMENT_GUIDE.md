# 🚀 Руководство по развертыванию на сервере

## 📋 Подготовка к развертыванию

### Требования

- Node.js 18+ 
- npm 9+
- PM2 (для управления процессом)
- Доступ к серверу по SSH

### Структура проекта

```
unified-bot/
├── src/                    # Исходный код TypeScript
├── ui/                     # Frontend файлы
├── dist/                   # Скомпилированный JavaScript (создается при сборке)
├── data/                   # Данные (отчеты сохраняются здесь)
├── backup/                 # Бекапы проекта
├── server.ts              # Главный файл сервера
├── package.json           # Зависимости и скрипты
├── tsconfig.json          # Конфигурация TypeScript
├── ecosystem.config.js    # Конфигурация PM2
└── deploy.sh              # Скрипт автоматического деплоя
```

## 🔧 Шаги развертывания

### 1. Подготовка сервера

```bash
# Обновление системы (Ubuntu/Debian)
sudo apt update && sudo apt upgrade -y

# Установка Node.js 18+
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs

# Установка PM2
sudo npm install -g pm2

# Проверка версий
node --version  # Должно быть >= 18.0.0
npm --version   # Должно быть >= 9.0.0
pm2 --version
```

### 2. Загрузка проекта на сервер

#### Вариант A: Через Git (рекомендуется)

```bash
# На сервере
cd /opt  # или другая директория
git clone <your-repository-url> unified-bot
cd unified-bot/unified-bot
```

#### Вариант B: Через SCP/SFTP

```bash
# С локальной машины
scp -r unified-bot user@server:/opt/
```

### 3. Установка зависимостей

```bash
cd /opt/unified-bot/unified-bot
npm install
```

### 4. Сборка проекта

```bash
npm run build
```

Проверьте, что папка `dist/` создана и содержит скомпилированные файлы.

### 5. Создание директории для данных

```bash
mkdir -p data
chmod 755 data
```

### 6. Настройка PM2

Проверьте файл `ecosystem.config.js`:

```javascript
module.exports = {
  apps: [{
    name: 'mexc-trading-bot',
    script: './dist/server.js',
    instances: 1,
    exec_mode: 'fork',
    env: {
      NODE_ENV: 'production',
      PORT: 3002
    },
    error_file: './logs/pm2-error.log',
    out_file: './logs/pm2-out.log',
    log_date_format: 'YYYY-MM-DD HH:mm:ss Z',
    merge_logs: true,
    autorestart: true,
    max_memory_restart: '1G'
  }]
};
```

Создайте директорию для логов:

```bash
mkdir -p logs
```

### 7. Запуск приложения

```bash
# Запуск через PM2
pm2 start ecosystem.config.js

# Или используя npm скрипт
npm run pm2:start

# Проверка статуса
pm2 status

# Просмотр логов
pm2 logs mexc-trading-bot

# Сохранение конфигурации PM2 для автозапуска
pm2 save
pm2 startup
```

### 8. Настройка автозапуска при перезагрузке сервера

```bash
# PM2 автоматически настроит автозапуск
pm2 startup
# Выполните команду, которую выведет PM2 (обычно с sudo)
```

### 9. Настройка файрвола (если нужно)

```bash
# Открыть порт 3002 (или другой, если изменили)
sudo ufw allow 3002/tcp
sudo ufw reload
```

### 10. Настройка Nginx (опционально, для домена)

Создайте конфигурацию Nginx:

```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://localhost:3002;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}
```

Перезагрузите Nginx:

```bash
sudo nginx -t
sudo systemctl reload nginx
```

## 🔄 Обновление приложения

### Автоматическое обновление (через deploy.sh)

```bash
# На сервере
cd /opt/unified-bot/unified-bot
bash deploy.sh
```

### Ручное обновление

```bash
# 1. Остановить приложение
pm2 stop mexc-trading-bot

# 2. Создать бекап (опционально)
cp -r . ../backup_$(date +%Y%m%d_%H%M%S)

# 3. Обновить код (если через Git)
git pull

# 4. Установить зависимости (если изменились)
npm install

# 5. Пересобрать проект
npm run build

# 6. Запустить приложение
pm2 restart mexc-trading-bot

# 7. Проверить логи
pm2 logs mexc-trading-bot --lines 50
```

## 📊 Мониторинг

### PM2 команды

```bash
# Статус приложения
pm2 status

# Детальная информация
pm2 describe mexc-trading-bot

# Логи в реальном времени
pm2 logs mexc-trading-bot

# Последние 100 строк логов
pm2 logs mexc-trading-bot --lines 100

# Мониторинг ресурсов
pm2 monit

# Перезапуск
pm2 restart mexc-trading-bot

# Остановка
pm2 stop mexc-trading-bot

# Удаление из PM2
pm2 delete mexc-trading-bot
```

### Проверка работы

```bash
# Проверка, что сервер отвечает
curl http://localhost:3002/api/health

# Должен вернуть: {"status":"ok","timestamp":"..."}
```

## 🗂️ Структура данных

### Файлы данных

- `data/account-reports.json` - отчеты по обработанным аккаунтам
  - Создается автоматически при первом сохранении отчета
  - Загружается при старте сервера
  - Сохраняется после каждого добавления/очистки отчета

### Логи

- `logs/pm2-error.log` - ошибки PM2
- `logs/pm2-out.log` - вывод приложения
- Консольные логи с префиксами:
  - `[TRADING]` - основная торговля
  - `[MULTI-ACCOUNT]` - мультиаккаунтинг
  - `[SPOT-API]` - мини-приложение (балансы и переводы)
  - `[REPORTS]` - отчеты

## ⚙️ Переменные окружения

Можно настроить через `ecosystem.config.js`:

```javascript
env: {
  NODE_ENV: 'production',
  PORT: 3002,
  HOST: '0.0.0.0'  // Для доступа извне
}
```

## 🔒 Безопасность

### Рекомендации:

1. **Не храните API ключи в коде**
   - Используйте переменные окружения или безопасное хранилище
   - API ключи в отчетах хранятся в `data/account-reports.json` - защитите этот файл

2. **Ограничьте доступ к серверу**
   ```bash
   # Только ваш IP может подключаться (пример)
   sudo ufw allow from YOUR_IP to any port 22
   sudo ufw allow from YOUR_IP to any port 3002
   ```

3. **Используйте HTTPS** (через Nginx + Let's Encrypt)
   ```bash
   sudo apt install certbot python3-certbot-nginx
   sudo certbot --nginx -d your-domain.com
   ```

4. **Регулярные бекапы**
   ```bash
   # Бекап данных
   tar -czf backup_data_$(date +%Y%m%d).tar.gz data/
   ```

## 🐛 Решение проблем

### Приложение не запускается

```bash
# Проверьте логи
pm2 logs mexc-trading-bot --err

# Проверьте, что порт свободен
sudo netstat -tulpn | grep 3002

# Проверьте права доступа
ls -la dist/server.js
```

### Ошибки компиляции

```bash
# Очистите и пересоберите
rm -rf dist/
npm run build
```

### Проблемы с памятью

```bash
# Увеличьте лимит памяти в ecosystem.config.js
max_memory_restart: '2G'
```

### Проблемы с данными

```bash
# Проверьте права на директорию data
chmod 755 data/
chown -R $USER:$USER data/
```

## 📝 Чеклист перед деплоем

- [ ] Node.js 18+ установлен
- [ ] npm 9+ установлен
- [ ] PM2 установлен
- [ ] Зависимости установлены (`npm install`)
- [ ] Проект собран (`npm run build`)
- [ ] Директория `data/` создана
- [ ] Директория `logs/` создана
- [ ] `ecosystem.config.js` настроен
- [ ] Порт открыт в файрволе (если нужно)
- [ ] Nginx настроен (если используется)
- [ ] Приложение запущено через PM2
- [ ] PM2 сохранен для автозапуска (`pm2 save`, `pm2 startup`)
- [ ] Проверен доступ к приложению
- [ ] Проверены логи на ошибки

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи: `pm2 logs mexc-trading-bot`
2. Проверьте статус: `pm2 status`
3. Проверьте документацию: `MINI_APP_DOCUMENTATION.md`
4. Проверьте бекапы в `backup/`

---

**Версия**: 1.0.0  
**Дата**: 2026-01-02  
**Статус**: Готово к развертыванию

