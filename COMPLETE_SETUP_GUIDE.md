# 🚀 Полное руководство по развертыванию на сервере

## 📋 Что мы будем делать

1. ✅ Подключимся к серверу
2. ✅ Установим все необходимое (Node.js, PM2, Nginx)
3. ✅ Загрузим приложение на сервер
4. ✅ Настроим приложение для работы
5. ✅ Запустим приложение через PM2 (автозапуск)
6. ✅ Настроим доступ через браузер (Nginx)
7. ✅ Настроим доступ для разработки и модификации кода

---

## 📧 ШАГ 1: Получение данных для подключения

После создания сервера на Vultr вы получите email с:
- **IP адрес сервера** (например: `45.76.123.45`)
- **Root пароль** (если не использовали SSH ключ)

**ВАЖНО:** Сохраните эти данные!

---

## 🔌 ШАГ 2: Подключение к серверу (Windows)

### Вариант A: Через PowerShell (рекомендуется)

1. Откройте **PowerShell** на вашем компьютере
2. Введите команду (замените `YOUR_SERVER_IP` на IP адрес вашего сервера):

```powershell
ssh root@YOUR_SERVER_IP
```

3. При первом подключении появится предупреждение:
   ```
   The authenticity of host '...' can't be established.
   Are you sure you want to continue connecting (yes/no)?
   ```
   Введите: **`yes`** и нажмите Enter

4. Введите **root пароль** (пароль не будет виден при вводе - это нормально)
5. Нажмите Enter

**Если подключились успешно**, вы увидите что-то вроде:
```
root@vultr:~#
```

### Вариант B: Через PuTTY (если PowerShell не работает)

1. Скачайте PuTTY: https://www.putty.org/
2. Откройте PuTTY
3. В поле "Host Name" введите: `root@YOUR_SERVER_IP`
4. Port: `22`
5. Нажмите "Open"
6. Введите пароль при запросе

---

## 🛠️ ШАГ 3: Первоначальная настройка сервера

Выполните эти команды **по порядку** (копируйте и вставляйте по одной):

### 3.1. Обновление системы

```bash
apt update && apt upgrade -y
```

Это займет 1-2 минуты. Дождитесь завершения.

### 3.2. Установка необходимых пакетов

```bash
apt install -y curl wget git build-essential ufw
```

### 3.3. Проверка ping к MEXC (проверим задержку)

```bash
ping -c 5 futures.mexc.com
```

Вы должны увидеть что-то вроде:
```
PING futures.mexc.com (52.84.124.xxx) 56(84) bytes of data.
64 bytes from 52.84.124.xxx: icmp_seq=1 time=25.3 ms
```

**Хорошая задержка:** 15-50ms из Японии

Нажмите `Ctrl+C` чтобы остановить ping.

---

## 📦 ШАГ 4: Установка Node.js

### 4.1. Добавление репозитория Node.js

```bash
curl -fsSL https://deb.nodesource.com/setup_18.x | bash -
```

### 4.2. Установка Node.js

```bash
apt-get install -y nodejs
```

### 4.3. Проверка установки

```bash
node --version
npm --version
```

**Должно показать:**
- `node --version`: `v18.x.x` или выше
- `npm --version`: `9.x.x` или выше

Если версии правильные - отлично! Продолжаем.

---

## 🔧 ШАГ 5: Установка PM2 (менеджер процессов)

PM2 нужен для:
- Автозапуска приложения при перезагрузке сервера
- Управления приложением (запуск, остановка, перезапуск)
- Просмотра логов

```bash
npm install -g pm2
```

Проверка:
```bash
pm2 --version
```

Должно показать версию PM2.

---

## 🌐 ШАГ 6: Установка Nginx (для доступа через браузер)

Nginx нужен для:
- Доступа к приложению через браузер (без указания порта)
- Проксирования запросов к приложению

```bash
apt install -y nginx
```

Проверка:
```bash
nginx -v
```

---

## 🔥 ШАГ 7: Настройка файрвола (важно!)

Файрвол защищает сервер. Нужно разрешить доступ к нужным портам.

### 7.1. Разрешаем SSH (ВАЖНО - делаем ПЕРВЫМ!)

```bash
ufw allow 22/tcp
```

**ВАЖНО:** Если не сделать это первым, можете потерять доступ к серверу!

### 7.2. Разрешаем HTTP (для Nginx)

```bash
ufw allow 80/tcp
```

### 7.3. Разрешаем HTTPS (для SSL в будущем)

```bash
ufw allow 443/tcp
```

### 7.4. Разрешаем порт приложения (на всякий случай)

```bash
ufw allow 3002/tcp
```

### 7.5. Включаем файрвол

```bash
ufw enable
```

При запросе введите: **`y`** и нажмите Enter

### 7.6. Проверка статуса

```bash
ufw status
```

Должно показать:
```
Status: active

To                         Action      From
--                         ------      ----
22/tcp                     ALLOW       Anywhere
80/tcp                     ALLOW       Anywhere
443/tcp                    ALLOW       Anywhere
3002/tcp                   ALLOW       Anywhere
```

---

## 📤 ШАГ 8: Загрузка приложения на сервер

Теперь нужно загрузить код приложения на сервер. Есть несколько способов.

### Способ A: Через SCP (рекомендуется для начала)

**На вашем локальном компьютере** (в PowerShell, в папке `D:\Cursors\uid`):

```powershell
scp -r unified-bot root@YOUR_SERVER_IP:/root/
```

Замените `YOUR_SERVER_IP` на IP адрес вашего сервера.

**Введите пароль** когда попросит.

Это займет 1-2 минуты (зависит от скорости интернета).

### Способ B: Через WinSCP (графический интерфейс)

1. Скачайте WinSCP: https://winscp.net/
2. Откройте WinSCP
3. Создайте новое подключение:
   - **Host name:** IP адрес сервера
   - **User name:** `root`
   - **Password:** пароль root
   - **Protocol:** SFTP
4. Нажмите "Login"
5. Перетащите папку `unified-bot` из левой панели (ваш компьютер) в правую панель (сервер) в папку `/root/`

### Проверка на сервере

**Вернитесь в SSH терминал** и выполните:

```bash
ls -la /root/
```

Должна быть папка `unified-bot`. Если есть - отлично!

```bash
cd /root/unified-bot
ls -la
```

Должны быть файлы: `package.json`, `server.ts`, `src/`, `ui/` и т.д.

---

## ⚙️ ШАГ 9: Установка зависимостей приложения

```bash
cd /root/unified-bot
npm install
```

Это займет 2-3 минуты. Дождитесь завершения.

---

## 🔨 ШАГ 10: Сборка приложения

```bash
npm run build
```

Это займет 30-60 секунд. Дождитесь завершения.

Проверка:
```bash
ls -la dist/
```

Должен быть файл `dist/server.js`.

---

## 📝 ШАГ 11: Создание файла окружения

```bash
cd /root/unified-bot
nano .env
```

Откроется редактор nano. Вставьте следующее:

```
PORT=3002
HOST=0.0.0.0
NODE_ENV=production
```

**Как работать с nano:**
- Вставьте текст (Ctrl+V или правая кнопка мыши)
- Сохранить: `Ctrl+O`, затем `Enter`
- Выйти: `Ctrl+X`

---

## 🚀 ШАГ 12: Запуск приложения через PM2

### 12.1. Запуск приложения

```bash
cd /root/unified-bot
pm2 start ecosystem.config.js
```

### 12.2. Проверка статуса

```bash
pm2 status
```

Должно показать что-то вроде:
```
┌─────┬──────────────────┬─────────┬─────────┬──────────┐
│ id  │ name             │ status  │ restart │ uptime   │
├─────┼──────────────────┼─────────┼─────────┼──────────┤
│ 0   │ mexc-trading-bot │ online  │ 0       │ 0s       │
└─────┴──────────────────┴─────────┴─────────┴──────────┘
```

Если статус `online` - отлично!

### 12.3. Просмотр логов

```bash
pm2 logs mexc-trading-bot
```

Вы увидите логи приложения. Нажмите `Ctrl+C` чтобы выйти.

### 12.4. Сохранение конфигурации PM2

```bash
pm2 save
```

### 12.5. Настройка автозапуска при перезагрузке сервера

```bash
pm2 startup
```

PM2 выведет команду, которую нужно выполнить. Обычно это что-то вроде:

```bash
sudo env PATH=$PATH:/usr/bin pm2 startup systemd -u root --hp /root
```

**Скопируйте и выполните эту команду** (она будет уникальной для вашего сервера).

---

## 🌐 ШАГ 13: Настройка Nginx для доступа через браузер

### 13.1. Создание конфигурации Nginx

```bash
nano /etc/nginx/sites-available/mexc-bot
```

Вставьте следующее (замените `YOUR_SERVER_IP` на IP адрес вашего сервера):

```nginx
server {
    listen 80;
    server_name YOUR_SERVER_IP;

    # Логи
    access_log /var/log/nginx/mexc-bot-access.log;
    error_log /var/log/nginx/mexc-bot-error.log;

    # Проксирование на приложение
    location / {
        proxy_pass http://localhost:3002;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        
        # Таймауты для WebSocket
        proxy_read_timeout 86400;
        proxy_send_timeout 86400;
    }
}
```

**Сохраните:** `Ctrl+O`, `Enter`, `Ctrl+X`

### 13.2. Активация конфигурации

```bash
ln -s /etc/nginx/sites-available/mexc-bot /etc/nginx/sites-enabled/
```

### 13.3. Проверка конфигурации

```bash
nginx -t
```

Должно показать:
```
nginx: the configuration file /etc/nginx/nginx.conf syntax is ok
nginx: configuration file /etc/nginx/nginx.conf test is successful
```

### 13.4. Перезапуск Nginx

```bash
systemctl restart nginx
```

### 13.5. Проверка статуса Nginx

```bash
systemctl status nginx
```

Должно показать `active (running)`. Нажмите `q` чтобы выйти.

---

## ✅ ШАГ 14: Проверка работы приложения

### 14.1. Проверка на сервере

```bash
curl http://localhost:3002/api/health
```

Должно вернуть: `{"status":"ok"}`

### 14.2. Проверка через браузер

Откройте в браузере на **любом компьютере**:

```
http://YOUR_SERVER_IP
```

Замените `YOUR_SERVER_IP` на IP адрес вашего сервера.

**Должен открыться интерфейс торгового бота!**

Если открывается - **отлично! Приложение работает!** 🎉

---

## 🔧 ШАГ 15: Настройка доступа для разработки

Теперь настроим доступ для модификации кода на сервере.

### 15.1. Создание пользователя для разработки (опционально, но рекомендуется)

```bash
adduser developer
```

Введите пароль для пользователя (запомните его!).

При запросе дополнительной информации просто нажимайте `Enter`.

### 15.2. Добавление пользователя в группу sudo

```bash
usermod -aG sudo developer
```

### 15.3. Настройка SSH доступа для пользователя

```bash
mkdir -p /home/developer/.ssh
chmod 700 /home/developer/.ssh
```

### 15.4. Переключение на пользователя

```bash
su - developer
```

Теперь вы работаете под пользователем `developer`.

---

## 📝 ШАГ 16: Полезные команды для управления

### Управление приложением через PM2

```bash
pm2 status                    # Статус приложения
pm2 logs mexc-trading-bot     # Просмотр логов
pm2 restart mexc-trading-bot  # Перезапуск
pm2 stop mexc-trading-bot      # Остановка
pm2 start mexc-trading-bot    # Запуск
pm2 monit                     # Мониторинг в реальном времени
```

### Просмотр логов

```bash
# Все логи
pm2 logs mexc-trading-bot

# Только ошибки
pm2 logs mexc-trading-bot --err

# Последние 100 строк
pm2 logs mexc-trading-bot --lines 100
```

### Проверка использования ресурсов

```bash
# Использование памяти и CPU
htop
# или
top
# (нажмите q чтобы выйти)

# Использование диска
df -h

# Использование памяти
free -h
```

### Перезапуск Nginx

```bash
sudo systemctl restart nginx
```

---

## 🔄 ШАГ 17: Обновление кода на сервере

Когда нужно обновить код на сервере:

### Способ A: Через SCP (замена файлов)

**На вашем локальном компьютере** (PowerShell):

```powershell
# Остановите приложение на сервере (через SSH)
# Затем загрузите новые файлы
scp -r unified-bot/src root@YOUR_SERVER_IP:/root/unified-bot/
scp unified-bot/server.ts root@YOUR_SERVER_IP:/root/unified-bot/
scp unified-bot/package.json root@YOUR_SERVER_IP:/root/unified-bot/
```

**На сервере:**

```bash
cd /root/unified-bot
npm install
npm run build
pm2 restart mexc-trading-bot
```

### Способ B: Через WinSCP

1. Подключитесь через WinSCP
2. Замените измененные файлы
3. На сервере выполните:
   ```bash
   cd /root/unified-bot
   npm run build
   pm2 restart mexc-trading-bot
   ```

### Способ C: Через Git (если настроен репозиторий)

```bash
cd /root/unified-bot
git pull
npm install
npm run build
pm2 restart mexc-trading-bot
```

---

## 🛠️ ШАГ 18: Настройка для работы с AI (модификация кода)

Для того, чтобы я (AI) мог помогать вам модифицировать код на сервере, нужно:

### 18.1. Убедиться, что SSH доступ работает

Проверьте, что вы можете подключиться:
```bash
# На вашем компьютере
ssh root@YOUR_SERVER_IP
```

### 18.2. Настройка для работы через Cursor/VS Code (опционально)

Если вы используете Cursor или VS Code, можно настроить Remote SSH:

1. Установите расширение "Remote - SSH" в Cursor/VS Code
2. Нажмите `F1` → "Remote-SSH: Connect to Host"
3. Введите: `root@YOUR_SERVER_IP`
4. Введите пароль
5. Теперь вы можете редактировать файлы на сервере прямо из редактора!

### 18.3. Рабочий процесс для модификации кода

**Вариант 1: Редактирование на сервере**

1. Подключитесь к серверу через SSH
2. Отредактируйте файлы через `nano` или через Remote SSH в Cursor
3. Пересоберите и перезапустите:
   ```bash
   cd /root/unified-bot
   npm run build
   pm2 restart mexc-trading-bot
   ```

**Вариант 2: Редактирование локально + загрузка на сервер**

1. Отредактируйте файлы на вашем компьютере
2. Загрузите на сервер через SCP/WinSCP
3. На сервере пересоберите и перезапустите

---

## 🔍 ШАГ 19: Решение проблем

### Приложение не запускается

```bash
# Проверьте логи
pm2 logs mexc-trading-bot --err

# Проверьте, что порт свободен
netstat -tulpn | grep 3002

# Проверьте права доступа
ls -la /root/unified-bot/dist/server.js
```

### Не могу подключиться через браузер

1. Проверьте файрвол:
   ```bash
   ufw status
   ```

2. Проверьте, что Nginx работает:
   ```bash
   systemctl status nginx
   ```

3. Проверьте логи Nginx:
   ```bash
   tail -f /var/log/nginx/mexc-bot-error.log
   ```

4. Проверьте, что приложение работает:
   ```bash
   pm2 status
   curl http://localhost:3002/api/health
   ```

### Высокое использование памяти

```bash
# Перезапустите приложение
pm2 restart mexc-trading-bot

# Проверьте использование
pm2 monit
```

### Проблемы с WebSocket

1. Проверьте настройки Nginx (таймауты)
2. Проверьте логи приложения:
   ```bash
   pm2 logs mexc-trading-bot
   ```

---

## 📊 ШАГ 20: Мониторинг и обслуживание

### Ежедневные проверки

```bash
# Статус приложения
pm2 status

# Использование ресурсов
htop
```

### Еженедельные проверки

```bash
# Обновление системы
apt update && apt upgrade -y

# Проверка логов на ошибки
pm2 logs mexc-trading-bot --err --lines 100
```

### Создание бекапов

```bash
cd /root
tar -czf backup_$(date +%Y%m%d_%H%M%S).tar.gz unified-bot --exclude=node_modules --exclude=dist
```

---

## ✅ Итоговая проверка

Убедитесь, что все работает:

- [ ] ✅ Подключение к серверу через SSH работает
- [ ] ✅ Node.js установлен (версия >= 18)
- [ ] ✅ PM2 установлен и работает
- [ ] ✅ Nginx установлен и работает
- [ ] ✅ Приложение запущено через PM2 (`pm2 status` показывает `online`)
- [ ] ✅ Приложение доступно через браузер (`http://YOUR_SERVER_IP`)
- [ ] ✅ Логи не показывают ошибок (`pm2 logs mexc-trading-bot`)
- [ ] ✅ Файрвол настроен (`ufw status`)

---

## 🎉 Готово!

Теперь ваше приложение:
- ✅ Работает на сервере
- ✅ Доступно через браузер с любого компьютера
- ✅ Автоматически запускается при перезагрузке сервера
- ✅ Готово к модификации и обновлению

**Для модификации кода:**
- Подключайтесь через SSH
- Редактируйте файлы
- Пересобирайте: `npm run build`
- Перезапускайте: `pm2 restart mexc-trading-bot`

**Для просмотра логов:**
```bash
pm2 logs mexc-trading-bot
```

**Для доступа через браузер:**
```
http://YOUR_SERVER_IP
```

---

## 📞 Если что-то не работает

1. Проверьте логи: `pm2 logs mexc-trading-bot --err`
2. Проверьте статус: `pm2 status`
3. Проверьте Nginx: `systemctl status nginx`
4. Проверьте файрвол: `ufw status`

**Все команды выполняются на сервере через SSH!**


