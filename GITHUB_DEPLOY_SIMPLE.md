# 🚀 Быстрая инструкция: GitHub → Сервер

## 📤 Загрузка на GitHub (локально)

### 1. Проверка изменений
```bash
cd D:\Cursors\uid\unified-bot
git status
```

### 2. Добавление всех изменений
```bash
git add .
```

### 3. Создание коммита
```bash
git commit -m "v2.1.0: Импорт аккаунтов, сброс статуса, таймаут между сделками, исправления блокировки"
```

### 4. Загрузка на GitHub
```bash
git push origin main
```

## 🖥️ Деплой на сервер

### Подключение к серверу и обновление
```bash
# 1. Подключиться к серверу
ssh user@your-server.com

# 2. Перейти в директорию проекта
cd /path/to/unified-bot

# 3. Получить изменения с GitHub
git pull origin main

# 4. Установить зависимости (если нужно)
npm install --production

# 5. Пересобрать проект
npm run build

# 6. Перезапустить приложение
pm2 restart mexc-trading-bot

# 7. Проверить статус
pm2 status
pm2 logs mexc-trading-bot --lines 20
```

## ✅ Готово!

После выполнения этих команд проект будет обновлен на сервере.
