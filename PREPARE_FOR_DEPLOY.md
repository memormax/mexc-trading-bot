# Подготовка к развертыванию

## ✅ Чеклист перед развертыванием

### 1. Проверка кода
- [ ] Все файлы скомпилированы (`npm run build`)
- [ ] Нет ошибок компиляции
- [ ] Тестирование на локальной машине пройдено

### 2. Файлы для развертывания
- [x] `.gitignore` - игнорирование ненужных файлов
- [x] `.env.example` - пример конфигурации
- [x] `ecosystem.config.js` - конфигурация PM2
- [x] `Dockerfile` - для Docker развертывания
- [x] `docker-compose.yml` - для Docker Compose
- [x] `deploy.sh` - скрипт автоматического развертывания

### 3. Документация
- [x] `README.md` - основная документация
- [x] `DEPLOYMENT.md` - руководство по развертыванию
- [x] `QUICK_START.md` - быстрый старт
- [x] `PERFORMANCE_TIMINGS.md` - анализ производительности
- [x] `CHANGELOG.md` - история изменений

### 4. Подготовка к загрузке на сервер

#### Вариант A: Через Git (рекомендуется)
```bash
# Инициализация Git (если еще не сделано)
git init
git add .
git commit -m "Initial commit"

# Добавление remote репозитория
git remote add origin <your-repo-url>
git push -u origin main
```

#### Вариант B: Через архив
```bash
# Создание архива (исключая node_modules, dist, .env)
tar -czf mexc-bot-deploy.tar.gz \
  --exclude=node_modules \
  --exclude=dist \
  --exclude=.env \
  --exclude=.git \
  --exclude=backup \
  --exclude=logs \
  .
```

#### Вариант C: Через SCP/SFTP
```bash
# Загрузка всех файлов кроме исключенных
scp -r \
  --exclude=node_modules \
  --exclude=dist \
  --exclude=.env \
  unified-bot/ user@server:/home/mexc-bot/
```

### 5. Что НЕ нужно загружать на сервер

- `node_modules/` - установится через `npm install`
- `dist/` - соберется через `npm run build`
- `.env` - создастся на сервере из `.env.example`
- `backup/` - локальные бекапы
- `.git/` - если не используете Git на сервере
- `logs/` - создастся автоматически

### 6. Проверка перед загрузкой

```bash
# Проверка размера проекта
du -sh unified-bot/

# Проверка структуры
tree -L 2 -I 'node_modules|dist' unified-bot/

# Проверка наличия всех необходимых файлов
ls -la unified-bot/ | grep -E "(package.json|tsconfig.json|server.ts|ecosystem.config.js)"
```

## 📦 Структура для развертывания

```
unified-bot/
├── src/                    # Исходный код (нужен для сборки)
├── ui/                     # Веб-интерфейс
├── server.ts              # Главный файл сервера
├── package.json           # Зависимости
├── tsconfig.json          # Конфигурация TypeScript
├── ecosystem.config.js    # PM2 конфигурация
├── Dockerfile             # Docker образ
├── docker-compose.yml     # Docker Compose
├── deploy.sh              # Скрипт развертывания
├── .gitignore             # Git ignore
├── .env.example           # Пример конфигурации
├── README.md              # Документация
├── DEPLOYMENT.md          # Руководство по развертыванию
├── QUICK_START.md         # Быстрый старт
└── PERFORMANCE_TIMINGS.md # Производительность
```

## 🚀 Быстрое развертывание

### На сервере выполните:

```bash
# 1. Загрузка файлов (через Git, SCP или SFTP)

# 2. Установка зависимостей
npm install --production

# 3. Сборка проекта
npm run build

# 4. Создание .env файла
cp .env.example .env
nano .env  # Настройте переменные

# 5. Запуск через PM2
pm2 start ecosystem.config.js
pm2 save
pm2 startup
```

Или используйте скрипт:
```bash
chmod +x deploy.sh
./deploy.sh
```

## 🔐 Безопасность

1. **Не загружайте `.env` файл** - создайте его на сервере
2. **Используйте HTTPS** - настройте SSL сертификат
3. **Ограничьте доступ** - используйте файрвол
4. **Регулярно обновляйте** - следите за обновлениями

## 📝 После развертывания

1. Проверьте статус: `pm2 status`
2. Проверьте логи: `pm2 logs mexc-trading-bot`
3. Проверьте здоровье: `curl http://localhost:3002/api/health`
4. Откройте в браузере: `http://your-server-ip:3002`

## 🆘 Если что-то пошло не так

1. Проверьте логи: `pm2 logs mexc-trading-bot --err`
2. Проверьте версию Node.js: `node --version`
3. Проверьте порт: `netstat -tulpn | grep 3002`
4. Проверьте права доступа: `ls -la dist/server.js`


