# 🚀 Быстрая инструкция: Загрузка на GitHub

## ⚠️ ВАЖНО: Правильный путь к проекту

Проект находится по адресу: **`D:\Cursors\uid\unified-bot`**

## 📤 Загрузка на GitHub

### 1. Откройте PowerShell или Git Bash и перейдите в правильную директорию:

```powershell
cd D:\Cursors\uid\unified-bot
```

### 2. Проверьте статус:

```powershell
git status
```

### 3. Добавьте все изменения:

```powershell
git add .
```

### 4. Создайте коммит:

```powershell
git commit -m "v2.1.0: Импорт аккаунтов, сброс статуса, таймаут между сделками, исправления блокировки"
```

### 5. Загрузите на GitHub:

```powershell
git push origin main
```

## 🖥️ Деплой на сервер

После загрузки на GitHub, на сервере выполните:

```bash
cd /path/to/unified-bot
git pull origin main
npm install --production
npm run build
pm2 restart mexc-trading-bot
pm2 status
```

## ✅ Готово!
