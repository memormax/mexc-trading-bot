# Быстрая настройка доступа по /flip

## Что изменилось:

- **Корневой путь (`/`)** - теперь показывает приветственную страницу с ссылкой на бота
- **Путь `/flip/`** - открывает торгового бота
- Все работает как на локальном сервере, так и на production

## На сервере выполните:

```bash
# 1. Создайте конфигурацию Nginx
sudo nano /etc/nginx/sites-available/mexc-bot-flip
```

Вставьте содержимое из файла `nginx-flip-config.conf` (замените `YOUR_SERVER_IP` на IP вашего сервера)

```bash
# 2. Активируйте конфигурацию
sudo ln -s /etc/nginx/sites-available/mexc-bot-flip /etc/nginx/sites-enabled/

# 3. Проверьте конфигурацию
sudo nginx -t

# 4. Перезагрузите Nginx
sudo systemctl reload nginx

# 5. Обновите код на сервере (если еще не обновлен)
cd /root/unified-bot
git pull origin main  # или скопируйте обновленные файлы
npm run build
pm2 restart mexc-trading-bot
```

## Проверка

Откройте в браузере: `http://ваш_сервер/flip`

Бот должен открыться корректно! 🎉

