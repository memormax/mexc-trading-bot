# 📤 Инструкция: Загрузка на GitHub и деплой на сервер

**Репозиторий:** https://github.com/memormax/mexc-trading-bot  
**Версия:** 2.1.0  
**Дата:** 2026-02-05 22:33:08

---

## 🚀 ШАГ 1: Загрузка на GitHub (на вашем компьютере)

### Откройте PowerShell или Git Bash в директории проекта:

```powershell
cd D:\Cursors\uid\unified-bot
```

### 1. Проверьте текущее состояние:

```bash
git status
```

Вы должны увидеть список измененных файлов.

### 2. Добавьте все изменения:

```bash
git add .
```

Это добавит все измененные и новые файлы (кроме тех, что в `.gitignore`).

### 3. Создайте коммит:

```bash
git commit -m "v2.1.0: Импорт аккаунтов, сброс статуса, таймаут между сделками, исправления блокировки и отображения ошибок"
```

Или более подробно:
```bash
git commit -m "v2.1.0: Обновление Flipbot

- Добавлен импорт аккаунтов из txt файла (как в Ferm)
- Добавлена кнопка сброса статуса аккаунта
- Добавлен таймаут между сделками
- Исправлена проблема с блокировкой бота в админке
- Исправлена проблема с отображением причины остановки аккаунта
- Улучшена обработка ошибок биржи
- Обновлена документация"
```

### 4. Загрузите на GitHub:

```bash
git push origin main
```

Если попросит авторизацию, введите ваш GitHub логин и токен доступа.

---

## 🖥️ ШАГ 2: Деплой на сервер

### Подключитесь к серверу по SSH:

```bash
ssh user@your-server.com
```

### Выполните следующие команды:

```bash
# 1. Перейдите в директорию проекта
cd /path/to/unified-bot

# 2. Получите последние изменения с GitHub
git pull origin main

# 3. Установите зависимости (если нужно)
npm install --production

# 4. Пересоберите проект
npm run build

# 5. Перезапустите приложение через PM2
pm2 restart mexc-trading-bot

# 6. Проверьте статус
pm2 status

# 7. Посмотрите логи (последние 50 строк)
pm2 logs mexc-trading-bot --lines 50
```

---

## ✅ Проверка после деплоя

### На сервере проверьте:

```bash
# Статус приложения
pm2 status

# Логи
pm2 logs mexc-trading-bot --lines 20

# Доступность API
curl http://localhost:3002/api/status
```

### В браузере проверьте:

- Главная страница: `http://your-server:3002/`
- Ferm Service: `http://your-server:3002/ferm/`
- Flipbot Service: `http://your-server:3002/flip/`
- Админ-панель: `http://your-server:3002/god/`

---

## 🔄 Автоматизация деплоя (опционально)

### Создайте скрипт на сервере для быстрого деплоя:

```bash
# На сервере создайте файл
nano /path/to/unified-bot/deploy-from-github.sh
```

Вставьте содержимое:
```bash
#!/bin/bash
set -e

echo "🔄 Начало обновления с GitHub..."

cd /path/to/unified-bot

echo "📥 Получение изменений с GitHub..."
git pull origin main

echo "📦 Установка зависимостей..."
npm install --production

echo "🔨 Сборка проекта..."
npm run build

echo "🔄 Перезапуск приложения..."
pm2 restart mexc-trading-bot

echo "✅ Обновление завершено!"
echo "📊 Статус:"
pm2 status

echo "📝 Последние логи:"
pm2 logs mexc-trading-bot --lines 10 --nostream
```

Сделайте скрипт исполняемым:
```bash
chmod +x /path/to/unified-bot/deploy-from-github.sh
```

Теперь для обновления достаточно:
```bash
./deploy-from-github.sh
```

---

## ⚠️ Важные замечания

### Что НЕ будет загружено на GitHub (благодаря .gitignore):

- ✅ `node_modules/` - зависимости (устанавливаются на сервере)
- ✅ `dist/` - скомпилированные файлы (собираются на сервере)
- ✅ `data/` - данные пользователей (остаются на сервере)
- ✅ `logs/` - логи (создаются на сервере)
- ✅ `backup/` - бекапы (локальные)

### Что БУДЕТ загружено:

- ✅ Исходный код (`.ts`, `.js`, `.html`, `.css`)
- ✅ Конфигурация (`package.json`, `tsconfig.json`, `ecosystem.config.js`)
- ✅ Документация (`.md` файлы)
- ✅ Скрипты (`deploy.sh`)

---

## 🐛 Решение проблем

### Проблема: "Permission denied" при push

**Решение:** Проверьте права доступа к репозиторию на GitHub. Убедитесь, что у вас есть права на запись.

### Проблема: "Updates were rejected"

**Решение:**
```bash
# Получите изменения с GitHub
git pull origin main --rebase

# Затем снова загрузите
git push origin main
```

### Проблема: Конфликты при pull на сервере

**Решение:**
```bash
# Сохраните локальные изменения (если есть)
git stash

# Получите изменения
git pull origin main

# Примените сохраненные изменения (если нужно)
git stash pop

# Разрешите конфликты вручную, затем:
git add .
git commit -m "Разрешение конфликтов"
```

### Проблема: Приложение не запускается после деплоя

**Решение:**
```bash
# Проверьте логи
pm2 logs mexc-trading-bot --err

# Проверьте, что сборка прошла успешно
ls -la dist/

# Пересоберите проект
npm run build

# Перезапустите
pm2 restart mexc-trading-bot
```

---

## 📝 Полная последовательность команд (копировать и выполнять)

### На вашем компьютере (Windows PowerShell):

```powershell
cd D:\Cursors\uid\unified-bot
git status
git add .
git commit -m "v2.1.0: Импорт аккаунтов, сброс статуса, таймаут между сделками, исправления"
git push origin main
```

### На сервере (Linux):

```bash
cd /path/to/unified-bot
git pull origin main
npm install --production
npm run build
pm2 restart mexc-trading-bot
pm2 status
```

---

## ✅ Готово!

После выполнения этих команд:
1. ✅ Все изменения загружены на GitHub
2. ✅ Проект обновлен на сервере
3. ✅ Приложение перезапущено с новыми изменениями

**Проверьте работу приложения в браузере!**
