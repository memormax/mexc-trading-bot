# ✅ Чеклист перед развертыванием на сервере

## 📦 Подготовка проекта

- [x] Бекап создан (`backup/backup_YYYY-MM-DD_HH-mm-ss/`)
- [x] Документация по мини-приложению создана (`MINI_APP_DOCUMENTATION.md`)
- [x] Руководство по развертыванию создано (`DEPLOYMENT_GUIDE.md`)
- [x] Проект собран (`npm run build`)
- [x] Все зависимости установлены (`npm install`)

## 🔍 Проверка файлов

- [x] `server.ts` - главный файл сервера
- [x] `src/spot-api-client.ts` - модуль мини-приложения
- [x] `ui/reports.html` - страница отчетов
- [x] `data/account-reports.json` - хранилище отчетов (создается автоматически)
- [x] `ecosystem.config.js` - конфигурация PM2
- [x] `deploy.sh` - скрипт автоматического деплоя
- [x] `package.json` - зависимости и скрипты

## 🚀 Готовность к деплою

### Локальная проверка

```bash
# 1. Сборка проекта
npm run build

# 2. Проверка, что dist/ создан
ls -la dist/

# 3. Проверка, что нет ошибок компиляции
# (должно завершиться без ошибок)

# 4. Тестовый запуск (опционально)
npm start
# Проверить http://localhost:3002
```

### Что нужно на сервере

- [ ] Node.js 18+ установлен
- [ ] npm 9+ установлен
- [ ] PM2 установлен (`npm install -g pm2`)
- [ ] Директория для проекта создана
- [ ] Файрвол настроен (порт 3002 открыт, если нужно)
- [ ] Nginx настроен (если используется домен)

## 📋 Шаги развертывания

1. **Загрузить проект на сервер**
   ```bash
   # Через Git или SCP
   ```

2. **Установить зависимости**
   ```bash
   cd /path/to/unified-bot
   npm install
   ```

3. **Собрать проект**
   ```bash
   npm run build
   ```

4. **Создать директории**
   ```bash
   mkdir -p data logs
   ```

5. **Запустить через PM2**
   ```bash
   pm2 start ecosystem.config.js
   pm2 save
   pm2 startup
   ```

6. **Проверить работу**
   ```bash
   pm2 status
   pm2 logs mexc-trading-bot
   curl http://localhost:3002/api/health
   ```

## 🔐 Безопасность

- [ ] API ключи не хранятся в коде
- [ ] Файл `data/account-reports.json` защищен (права доступа)
- [ ] Файрвол настроен (только нужные порты открыты)
- [ ] HTTPS настроен (если используется домен)
- [ ] Регулярные бекапы настроены

## 📊 После развертывания

- [ ] Приложение доступно по адресу
- [ ] Страница отчетов открывается
- [ ] Мини-приложение работает (проверка балансов)
- [ ] Переводы работают
- [ ] Логи пишутся корректно
- [ ] PM2 автозапуск настроен

## 🐛 В случае проблем

1. Проверить логи: `pm2 logs mexc-trading-bot`
2. Проверить статус: `pm2 status`
3. Проверить документацию: `DEPLOYMENT_GUIDE.md`
4. Проверить бекапы: `backup/`

---

**Дата создания**: 2026-01-02  
**Версия проекта**: 1.0.0  
**Статус**: ✅ Готово к развертыванию









