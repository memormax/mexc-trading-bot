# 🔍 Проверка сервера - отчеты не работают

## Проблема
При запросе `curl http://localhost:3002/api/account-reports` возвращается HTML вместо JSON.

## Причина
Код на сервере не обновлен или не пересобран.

## Решение

### 1. Проверить, что код обновлен из GitHub

```bash
cd /root/unified-bot

# Проверить статус git
git status

# Если есть изменения, получить их
git pull origin main

# Проверить, что файл server.ts обновлен
grep -n "account-reports" server.ts
```

Должно быть:
```
4398:app.get('/api/account-reports', (req, res) => {
```

### 2. Пересобрать проект

```bash
# Очистить старую сборку
rm -rf dist/

# Пересобрать
npm run build

# Проверить, что файл собран
ls -la dist/server.js
```

### 3. Проверить скомпилированный код

```bash
# Проверить, что роут есть в скомпилированном файле
grep -n "account-reports" dist/server.js
```

Должно найти строку с `app.get('/api/account-reports'`.

### 4. Перезапустить PM2

```bash
# Остановить
pm2 stop mexc-trading-bot

# Удалить из PM2
pm2 delete mexc-trading-bot

# Запустить заново
cd /root/unified-bot
pm2 start ecosystem.config.js

# Сохранить конфигурацию
pm2 save

# Проверить статус
pm2 status
```

### 5. Проверить логи

```bash
# Посмотреть логи запуска
pm2 logs mexc-trading-bot --lines 50

# Должно быть:
# [REPORTS] 🔍 Путь к файлу отчетов: /root/unified-bot/data/account-reports.json
```

### 6. Проверить API снова

```bash
# Проверить endpoint
curl http://localhost:3002/api/account-reports

# Должен вернуть JSON:
# {"success":true,"data":[]}
```

## Если все еще не работает

### Проверить, что сервер запущен с правильного места

```bash
# Проверить, откуда запущен процесс
pm2 info mexc-trading-bot | grep "exec cwd"

# Должно быть: /root/unified-bot
```

### Проверить версию Node.js

```bash
node -v
# Должно быть: v18.x.x или выше
```

### Проверить, что порт правильный

```bash
# Проверить, на каком порту слушает
netstat -tulpn | grep node

# Или
pm2 info mexc-trading-bot | grep "port"
```

## Быстрая проверка всех шагов

```bash
cd /root/unified-bot && \
git pull origin main && \
npm run build && \
pm2 restart mexc-trading-bot && \
sleep 3 && \
curl http://localhost:3002/api/account-reports
```









