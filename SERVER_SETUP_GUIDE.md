# Пошаговое руководство по настройке сервера

## 🎯 Выбор провайдера

### Топ-3 рекомендации для MEXC бота:

1. **Vultr Tokyo** - Лучший выбор ($12/месяц)
2. **Linode Tokyo** - Отличная альтернатива ($12/месяц)
3. **DigitalOcean Singapore** - Простота использования ($12/месяц)

## 📋 Пошаговая инструкция (Vultr)

### Шаг 1: Регистрация и создание сервера

1. Перейдите на https://www.vultr.com/
2. Нажмите "Sign Up" и зарегистрируйтесь
3. Добавьте способ оплаты (карта или PayPal)
4. Нажмите "Deploy Server" или "+" → "Deploy Server"

### Шаг 2: Выбор конфигурации

**Рекомендуемые настройки:**

- **Server Type**: Cloud Compute
- **CPU & Storage Technology**: Regular Performance
- **Server Location**: 
  - 🇯🇵 **Tokyo, Japan** (рекомендуется для MEXC)
  - 🇸🇬 **Singapore** (альтернатива)
- **Operating System**: 
  - Ubuntu 22.04 LTS (рекомендуется)
  - или Ubuntu 20.04 LTS
- **Server Plan**: 
  - **$12/month** - 1 vCPU, 2GB RAM, 55GB SSD (рекомендуется)
  - **$6/month** - 1 vCPU, 1GB RAM, 25GB SSD (минимальная)
- **Additional Features**: 
  - ✅ Enable IPv6 (опционально)
  - ✅ Auto Backups (опционально, +20% к цене)
- **SSH Keys**: Добавьте ваш SSH ключ (рекомендуется) или используйте пароль

### Шаг 3: Развертывание

1. Нажмите "Deploy Now"
2. Дождитесь создания сервера (1-2 минуты)
3. Запишите:
   - **IP адрес** сервера
   - **Root пароль** (если не использовали SSH ключ)

### Шаг 4: Подключение к серверу

#### Через SSH (Windows - PowerShell или PuTTY):

```bash
# Если используете пароль
ssh root@YOUR_SERVER_IP

# Если используете SSH ключ
ssh -i ~/.ssh/your_key root@YOUR_SERVER_IP
```

#### Первое подключение:
- При запросе введите "yes" для подтверждения fingerprint
- Введите root пароль (если используете пароль)

### Шаг 5: Первоначальная настройка

```bash
# 1. Обновление системы
apt update && apt upgrade -y

# 2. Установка необходимых пакетов
apt install -y curl wget git build-essential

# 3. Проверка ping к MEXC (должен быть < 50ms из Японии)
ping -c 5 futures.mexc.com

# 4. Создание пользователя для приложения
adduser mexc-bot
usermod -aG sudo mexc-bot

# 5. Переключение на нового пользователя
su - mexc-bot
```

### Шаг 6: Установка Node.js

```bash
# Установка Node.js 18.x через NodeSource
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs

# Проверка установки
node --version  # Должно быть >= 18.0.0
npm --version   # Должно быть >= 9.0.0
```

### Шаг 7: Установка PM2

```bash
sudo npm install -g pm2
```

### Шаг 8: Настройка файрвола

```bash
# Разрешаем SSH (важно сделать ПЕРВЫМ!)
sudo ufw allow 22/tcp

# Разрешаем порт приложения
sudo ufw allow 3002/tcp

# Или если используете Nginx
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp

# Включаем файрвол
sudo ufw enable

# Проверка статуса
sudo ufw status
```

### Шаг 9: Загрузка приложения

#### Вариант A: Через Git

```bash
# Клонирование репозитория
git clone <your-repo-url> mexc-trading-bot
cd mexc-trading-bot
```

#### Вариант B: Через SCP (с локальной машины)

```bash
# На локальной машине (Windows PowerShell)
scp -r unified-bot mexc-bot@YOUR_SERVER_IP:~/

# На сервере
cd ~/unified-bot
```

#### Вариант C: Через SFTP (FileZilla, WinSCP)

1. Подключитесь к серверу через SFTP
2. Загрузите папку `unified-bot` в домашнюю директорию пользователя `mexc-bot`

### Шаг 10: Установка приложения

```bash
cd ~/mexc-trading-bot  # или ~/unified-bot

# Установка зависимостей
npm install --production

# Сборка проекта
npm run build

# Создание директорий
mkdir -p logs backup
```

### Шаг 11: Настройка окружения

```bash
# Создание .env файла
cp .env.example .env
nano .env
```

Настройте переменные:
```env
PORT=3002
HOST=0.0.0.0
NODE_ENV=production
```

Сохраните: `Ctrl+O`, `Enter`, `Ctrl+X`

### Шаг 12: Запуск через PM2

```bash
# Запуск приложения
pm2 start ecosystem.config.js

# Сохранение конфигурации PM2
pm2 save

# Настройка автозапуска при перезагрузке
pm2 startup
# Выполните команду, которую выведет PM2 (обычно с sudo)
```

### Шаг 13: Проверка работы

```bash
# Проверка статуса
pm2 status

# Просмотр логов
pm2 logs mexc-trading-bot

# Проверка API
curl http://localhost:3002/api/health
# Должен вернуть: {"status":"ok"}
```

### Шаг 14: Настройка Nginx (опционально, но рекомендуется)

```bash
# Установка Nginx
sudo apt install -y nginx

# Создание конфигурации
sudo nano /etc/nginx/sites-available/mexc-bot
```

Вставьте конфигурацию:
```nginx
server {
    listen 80;
    server_name YOUR_SERVER_IP;  # или ваш домен

    location / {
        proxy_pass http://localhost:3002;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        
        proxy_read_timeout 86400;
        proxy_send_timeout 86400;
    }
}
```

Активация:
```bash
sudo ln -s /etc/nginx/sites-available/mexc-bot /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx
```

### Шаг 15: Настройка SSL (опционально, но рекомендуется)

```bash
# Установка Certbot
sudo apt install -y certbot python3-certbot-nginx

# Получение сертификата (если есть домен)
sudo certbot --nginx -d your-domain.com

# Автоматическое обновление
sudo certbot renew --dry-run
```

## ✅ Проверка задержки к MEXC

После настройки проверьте задержку:

```bash
# Ping тест
ping -c 10 futures.mexc.com

# HTTP тест
curl -o /dev/null -s -w "Time: %{time_total}s\n" \
  https://futures.mexc.com/api/v1/contract/ticker?symbol=UNI_USDT
```

**Ожидаемые результаты:**
- Из Токио: **15-30ms** ping, **50-100ms** HTTP
- Из Сингапура: **10-25ms** ping, **40-80ms** HTTP

## 🔒 Безопасность

### Дополнительные меры безопасности:

1. **Отключение root логина через пароль:**
```bash
sudo nano /etc/ssh/sshd_config
# Установите: PasswordAuthentication no
sudo systemctl restart sshd
```

2. **Настройка fail2ban:**
```bash
sudo apt install -y fail2ban
sudo systemctl enable fail2ban
sudo systemctl start fail2ban
```

3. **Регулярные обновления:**
```bash
# Добавьте в crontab
sudo crontab -e
# Добавьте: 0 3 * * * apt update && apt upgrade -y
```

## 📊 Мониторинг

### Полезные команды:

```bash
# Статус приложения
pm2 status
pm2 monit

# Использование ресурсов
htop
# или
top

# Использование диска
df -h

# Использование памяти
free -h

# Сетевые соединения
netstat -tulpn
```

## 🆘 Решение проблем

### Приложение не запускается:
```bash
pm2 logs mexc-trading-bot --err
```

### Порт занят:
```bash
sudo netstat -tulpn | grep 3002
sudo lsof -i :3002
```

### Высокое использование памяти:
```bash
pm2 restart mexc-trading-bot
```

### Проблемы с сетью:
```bash
# Проверка DNS
nslookup futures.mexc.com

# Проверка маршрута
traceroute futures.mexc.com
```

## 📝 Полезные ссылки

- Vultr документация: https://www.vultr.com/docs/
- PM2 документация: https://pm2.keymetrics.io/docs/
- Nginx документация: https://nginx.org/en/docs/

## 🎯 Итоговая проверка

После настройки убедитесь, что:

- [ ] Сервер создан и доступен
- [ ] Node.js установлен (версия >= 18)
- [ ] Приложение установлено и собрано
- [ ] PM2 запущен и приложение работает
- [ ] Файрвол настроен
- [ ] Ping к MEXC < 50ms
- [ ] Приложение доступно через браузер
- [ ] Логи не показывают ошибок

## 🚀 Готово!

Теперь ваше приложение развернуто и готово к работе. Откройте в браузере:
- `http://YOUR_SERVER_IP:3002` (если не используете Nginx)
- `http://YOUR_SERVER_IP` (если используете Nginx)


