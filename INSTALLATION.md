# Руководство по установке

## 📋 Содержание

1. [Требования](#требования)
2. [Установка на Windows](#установка-на-windows)
3. [Установка на Linux](#установка-на-linux)
4. [Установка на macOS](#установка-на-macos)
5. [Установка через Docker](#установка-через-docker)
6. [Проверка установки](#проверка-установки)

## 🔧 Требования

### Минимальные требования:
- **Node.js**: версия 18.0.0 или выше
- **npm**: версия 9.0.0 или выше
- **Операционная система**: Windows 10+, Linux, macOS
- **RAM**: минимум 512 MB
- **Диск**: минимум 1 GB свободного места

### Проверка версий:
```bash
node --version  # Должно быть >= 18.0.0
npm --version   # Должно быть >= 9.0.0
```

## 🪟 Установка на Windows

### Шаг 1: Установка Node.js

1. Скачайте Node.js с официального сайта: https://nodejs.org/
2. Выберите версию LTS (рекомендуется 18.x или выше)
3. Запустите установщик и следуйте инструкциям
4. Убедитесь, что опция "Add to PATH" отмечена

### Шаг 2: Проверка установки

Откройте PowerShell или Command Prompt:
```powershell
node --version
npm --version
```

### Шаг 3: Установка приложения

```powershell
# Перейдите в директорию проекта
cd unified-bot

# Установка зависимостей
npm install

# Сборка проекта
npm run build
```

### Шаг 4: Запуск

```powershell
npm start
```

Приложение будет доступно по адресу: `http://localhost:3002`

## 🐧 Установка на Linux

### Шаг 1: Установка Node.js

#### Ubuntu/Debian:
```bash
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs
```

#### CentOS/RHEL:
```bash
curl -fsSL https://rpm.nodesource.com/setup_18.x | sudo bash -
sudo yum install -y nodejs
```

#### Через nvm (рекомендуется):
```bash
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash
source ~/.bashrc
nvm install 18
nvm use 18
```

### Шаг 2: Установка приложения

```bash
# Перейдите в директорию проекта
cd unified-bot

# Установка зависимостей
npm install

# Сборка проекта
npm run build
```

### Шаг 3: Запуск

```bash
npm start
```

Или через PM2 (для production):
```bash
npm install -g pm2
pm2 start ecosystem.config.js
```

## 🍎 Установка на macOS

### Шаг 1: Установка Node.js

#### Через Homebrew (рекомендуется):
```bash
brew install node@18
```

#### Через официальный установщик:
1. Скачайте с https://nodejs.org/
2. Запустите установщик

#### Через nvm:
```bash
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash
source ~/.zshrc
nvm install 18
nvm use 18
```

### Шаг 2: Установка приложения

```bash
cd unified-bot
npm install
npm run build
```

### Шаг 3: Запуск

```bash
npm start
```

## 🐳 Установка через Docker

### Требования:
- Docker версии 20.10 или выше
- Docker Compose версии 1.29 или выше

### Шаг 1: Сборка образа

```bash
docker build -t mexc-trading-bot .
```

### Шаг 2: Запуск через Docker Compose

```bash
docker-compose up -d
```

### Шаг 3: Просмотр логов

```bash
docker-compose logs -f mexc-bot
```

### Шаг 4: Остановка

```bash
docker-compose down
```

## ✅ Проверка установки

### 1. Проверка версий

```bash
node --version  # Должно быть >= 18.0.0
npm --version   # Должно быть >= 9.0.0
```

### 2. Проверка сборки

```bash
npm run build
# Должно завершиться без ошибок
ls dist/server.js  # Файл должен существовать
```

### 3. Проверка запуска

```bash
npm start
# В браузере откройте: http://localhost:3002
# Должна открыться страница приложения
```

### 4. Проверка API

```bash
curl http://localhost:3002/api/health
# Должен вернуть: {"status":"ok"}
```

## 🔧 Решение проблем

### Ошибка "Node.js not found"
→ Установите Node.js с официального сайта или через менеджер пакетов

### Ошибка "npm not found"
→ Node.js должен включать npm. Переустановите Node.js

### Ошибка при сборке
→ Проверьте версию Node.js (должна быть >= 18.0.0)
→ Удалите `node_modules` и `dist`, затем выполните `npm install` заново

### Порт 3002 занят
→ Измените порт в `.env` файле или остановите другое приложение на этом порту

### Ошибки зависимостей
→ Удалите `node_modules` и `package-lock.json`, затем выполните `npm install` заново

## 📚 Дополнительная информация

- Быстрый старт: [QUICK_START.md](./QUICK_START.md)
- Развертывание на сервере: [DEPLOYMENT.md](./DEPLOYMENT.md)
- Основная документация: [README.md](./README.md)


