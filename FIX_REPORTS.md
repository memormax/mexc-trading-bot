# 🔧 Исправление проблемы с загрузкой отчетов

## Проблема
Отчеты не загружаются на сервере из-за неправильного пути к файлу `data/account-reports.json`.

## Решение

### 1. Обновить код на сервере

```bash
ssh root@45.32.53.81
cd /root/unified-bot

# Получить последние изменения
git pull origin main

# Пересобрать проект
npm run build

# Перезапустить бота
pm2 restart mexc-trading-bot
```

### 2. Проверить логи

```bash
# Посмотреть логи загрузки отчетов
pm2 logs mexc-trading-bot | grep -i "REPORTS"

# Или все логи
pm2 logs mexc-trading-bot --lines 100
```

### 3. Проверить файл отчетов

```bash
# Проверить, существует ли файл
ls -la /root/unified-bot/data/account-reports.json

# Если файла нет, создать директорию
mkdir -p /root/unified-bot/data

# Проверить права доступа
chmod 755 /root/unified-bot/data
```

### 4. Проверить работу API

```bash
# Проверить endpoint
curl http://localhost:3002/api/account-reports
```

## Что было исправлено

1. **Путь к файлу**: Изменен с `path.join(__dirname, '..', 'data', ...)` на `path.join(process.cwd(), 'data', ...)`
   - Это гарантирует, что путь всегда указывает на корень проекта, а не на `dist/`

2. **Логирование**: Добавлено детальное логирование для диагностики:
   - Путь к файлу отчетов
   - Текущая рабочая директория
   - Количество загруженных отчетов

## После исправления

После перезапуска бота в логах должно появиться:
```
[REPORTS] 🔍 Путь к файлу отчетов: /root/unified-bot/data/account-reports.json
[REPORTS] ✅ Файл отчетов найден
[REPORTS] ✅ Загружено X отчетов из файла
```

Или если файла нет:
```
[REPORTS] 📁 Создаем директорию для отчетов: /root/unified-bot/data
[REPORTS] ✅ Директория создана
```

