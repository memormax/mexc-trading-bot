# 🤝 Как мы работаем вместе (GitHub + Сервер)

## 📋 Обзор

Мы используем **GitHub** для хранения кода и синхронизации между локальным компьютером и сервером. Я (AI) помогаю вам модифицировать код, а вы загружаете изменения через Git.

---

## 🚀 Настройка GitHub (если еще не настроено)

### Шаг 1: Создайте репозиторий на GitHub

1. Зайдите на https://github.com
2. Нажмите **"+"** → **"New repository"**
3. Заполните:
   - **Repository name:** `mexc-trading-bot`
   - **Visibility:** Private (рекомендуется)
   - **НЕ ставьте галочки** на "Add a README file", "Add .gitignore"
4. Нажмите **"Create repository"**

### Шаг 2: Подключите локальный репозиторий

```powershell
cd D:\Cursors\uid\unified-bot

# Если git еще не инициализирован
git init
git add .
git commit -m "Initial commit"

# Подключите к GitHub (замените YOUR_USERNAME)
git remote add origin https://github.com/YOUR_USERNAME/mexc-trading-bot.git
git branch -M main
git push -u origin main
```

**При push потребуется Personal Access Token** (не пароль!):
- GitHub → Settings → Developer settings → Personal access tokens → Generate new token
- Выберите права: `repo`

### Шаг 3: Настройте сервер

```bash
cd /root
rm -rf unified-bot
git clone https://github.com/YOUR_USERNAME/mexc-trading-bot.git unified-bot
cd unified-bot
npm install
npm run build
pm2 start ecosystem.config.js
pm2 save
```

---

## 🔄 Как мы работаем вместе

Я не могу напрямую подключаться к вашему серверу, но мы можем эффективно работать вместе следующими способами:

---

## 🔄 Способ 1: Через вас (рекомендуется)

### Как это работает:

1. **Вы подключаетесь к серверу через SSH:**
   ```powershell
   ssh root@YOUR_SERVER_IP
   ```

2. **Вы говорите мне, что нужно изменить** (например: "Исправь баг с комиссией")

3. **Я даю вам команды и код**, которые нужно выполнить/вставить

4. **Вы выполняете команды на сервере** и сообщаете результат

5. **Я проверяю результат** и даю следующие инструкции

### Пример работы:

**Вы:** "Нужно исправить баг с отображением комиссии"

**Я:** 
```bash
# 1. Откройте файл для редактирования
nano /root/unified-bot/ui/app.js

# 2. Найдите строку (примерно строка 150):
# let fee = parseFloat(order.fee || 0);

# 3. Замените на:
let fee = parseFloat(order.totalFee || order.makerFee + order.takerFee || order.fee || 0);

# 4. Сохраните (Ctrl+O, Enter, Ctrl+X)
```

**Вы:** Выполняете команды и сообщаете результат

**Я:** 
```bash
# Теперь пересоберите и перезапустите:
cd /root/unified-bot
npm run build
pm2 restart mexc-trading-bot
```

---

## 🔄 Способ 2: Редактирование локально + загрузка на сервер

### Как это работает:

1. **Я помогаю вам редактировать файлы на вашем локальном компьютере** (в папке `D:\Cursors\uid\unified-bot`)

2. **Вы загружаете измененные файлы на сервер** через SCP или WinSCP

3. **На сервере пересобираете и перезапускаете:**
   ```bash
   cd /root/unified-bot
   npm run build
   pm2 restart mexc-trading-bot
   ```

### Пример работы:

**Вы:** "Нужно добавить новую функцию"

**Я:** Редактирую файлы на вашем локальном компьютере через инструменты редактирования

**Вы:** Загружаете файлы на сервер:
```powershell
scp unified-bot/src/trading-handler.ts root@YOUR_SERVER_IP:/root/unified-bot/src/
```

**Я:** 
```bash
# На сервере:
cd /root/unified-bot
npm run build
pm2 restart mexc-trading-bot
pm2 logs mexc-trading-bot --lines 20
```

---

## 🔄 Способ 3: Через Remote SSH в Cursor (если настроено)

### Как это работает:

1. **Настройте Remote SSH в Cursor:**
   - Установите расширение "Remote - SSH"
   - Подключитесь к серверу: `root@YOUR_SERVER_IP`

2. **Теперь я могу редактировать файлы на сервере напрямую** через Cursor

3. **После редактирования пересобираем и перезапускаем:**
   ```bash
   npm run build
   pm2 restart mexc-trading-bot
   ```

### Преимущества:
- ✅ Я вижу файлы на сервере
- ✅ Могу редактировать напрямую
- ✅ Быстрее процесс разработки

---

## 📝 Типичные сценарии работы

### Сценарий 1: Исправление бага

1. **Вы:** "Есть баг: комиссия не отображается"
2. **Я:** "Давайте проверим логи"
3. **Вы:** Выполняете `pm2 logs mexc-trading-bot --err`
4. **Я:** Вижу ошибку, даю команды для исправления
5. **Вы:** Выполняете команды
6. **Я:** Проверяю результат, даю команды для перезапуска
7. **Вы:** Перезапускаете и проверяете

### Сценарий 2: Добавление новой функции

1. **Вы:** "Нужно добавить функцию X"
2. **Я:** Редактирую файлы на вашем компьютере
3. **Вы:** Загружаете файлы на сервер
4. **Я:** Даю команды для сборки и перезапуска
5. **Вы:** Выполняете команды
6. **Я:** Проверяю логи, даю инструкции по тестированию

### Сценарий 3: Оптимизация кода

1. **Вы:** "Код работает медленно"
2. **Я:** Анализирую код, нахожу узкие места
3. **Я:** Предлагаю оптимизации, редактирую код
4. **Вы:** Загружаете на сервер
5. **Я:** Даю команды для тестирования производительности

---

## 🛠️ Что я могу делать

✅ **Могу:**
- Редактировать файлы на вашем локальном компьютере
- Давать команды для выполнения на сервере
- Анализировать логи и ошибки
- Помогать с отладкой
- Оптимизировать код
- Создавать новые функции

❌ **Не могу:**
- Напрямую подключаться к серверу
- Выполнять команды на сервере без вас
- Видеть файлы на сервере (если не настроен Remote SSH)

---

## 💡 Рекомендации для эффективной работы

### 1. Всегда проверяйте логи после изменений

```bash
pm2 logs mexc-trading-bot --lines 50
```

### 2. Сообщайте мне результаты выполнения команд

Например:
- "Команда выполнена успешно"
- "Ошибка: файл не найден"
- "Логи показывают: ..."

### 3. Создавайте бекапы перед большими изменениями

```bash
cd /root
tar -czf backup_$(date +%Y%m%d_%H%M%S).tar.gz unified-bot --exclude=node_modules --exclude=dist
```

### 4. Используйте понятные описания проблем

Вместо: "Не работает"
Лучше: "При открытии позиции появляется ошибка в логах: ..."

---

## 🔍 Как сообщать о проблемах

### Хороший отчет о проблеме:

```
Проблема: Комиссия не отображается в истории сделок

Шаги для воспроизведения:
1. Открыл позицию
2. Закрыл позицию
3. Обновил историю сделок
4. Комиссия показывает $0.0000

Логи:
pm2 logs mexc-trading-bot --err показал:
[ERROR] Fee calculation failed: undefined

Ожидаемое поведение:
Комиссия должна отображаться как $0.1234
```

### Плохой отчет о проблеме:

```
Не работает
```

---

## 🚀 Быстрый старт работы

1. **Подключитесь к серверу:**
   ```powershell
   ssh root@YOUR_SERVER_IP
   ```

2. **Сообщите мне, что нужно сделать**

3. **Выполняйте команды, которые я даю**

4. **Сообщайте результаты**

5. **Проверяйте работу после изменений**

---

## 📞 Если что-то пошло не так

1. **Не паникуйте!** Все можно исправить
2. **Сообщите мне точную ошибку** из логов
3. **Создайте бекап** текущего состояния
4. **Я помогу восстановить** рабочее состояние

---

## ✅ Итог

**Мы работаем вместе:**
- Вы подключаетесь к серверу
- Я даю команды и код
- Вы выполняете
- Я проверяю результат
- Повторяем до готовности

**Это эффективный способ работы!** 🎉


