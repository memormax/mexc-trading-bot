# 📊 Принцип и логика работы Автообъема

## 🎯 Общая концепция

**Автообъем** - это функция, которая автоматически рассчитывает оптимальный объем позиции на основе текущего баланса счета, плеча и заданных параметров. Это позволяет максимально эффективно использовать доступный капитал для торговли.

---

## 🔧 Как это работает

### 1. **Исходные данные**

При расчете автообъема используются следующие данные:

- **Доступный баланс USDT** - получается из API MEXC в реальном времени
- **Авто плечо** - значение из настроек (по умолчанию 10x)
- **Процент от баланса** - процент от максимально возможного объема (по умолчанию 90%)
- **Максимальный объем** - ограничение максимальной суммы для торговли (по умолчанию 3500 USDT)

### 2. **Формула расчета**

```
Шаг 1: Максимально возможный объем = Доступный баланс × Плечо

Шаг 2: Объем с учетом процента = Максимально возможный объем × (Процент / 100)

Шаг 3: Финальный объем = MIN(Объем с учетом процента, Максимальный объем)
```

### 3. **Пример расчета**

**Исходные данные:**
- Доступный баланс: 500 USDT
- Авто плечо: 10x
- Процент от баланса: 90%
- Максимальный объем: 3500 USDT

**Расчет:**
```
Шаг 1: Максимально возможный объем = 500 × 10 = 5000 USDT

Шаг 2: Объем с учетом процента = 5000 × (90 / 100) = 4500 USDT

Шаг 3: Финальный объем = MIN(4500, 3500) = 3500 USDT
```

**Результат:** Будет использоваться объем **3500 USDT** для торговли.

---

## ⚙️ Когда происходит расчет

### 1. **При запуске бота**

Когда вы нажимаете кнопку "Запустить" и автообъем включен:
- Бот получает текущий баланс из API
- Рассчитывает оптимальный объем по формуле
- Устанавливает этот объем для всех последующих сделок

### 2. **Каждые 3 сделки**

После закрытия каждой позиции:
- Увеличивается счетчик сделок
- Когда счетчик достигает 3, происходит пересчет объема
- Счетчик сбрасывается в 0
- Новый объем применяется для следующих сделок

**Почему каждые 3 сделки?**
- Баланс может изменяться после каждой сделки (прибыль/убыток)
- Частый пересчет может замедлить работу бота
- 3 сделки - оптимальный баланс между актуальностью и производительностью

### 3. **При изменении настроек**

Если вы изменили настройки автообъема (процент, максимальный объем) и нажали "Сохранить":
- Объем пересчитывается немедленно
- Новые параметры применяются сразу

---

## 🚀 Оптимизация производительности

### Асинхронный расчет

Расчет автообъема выполняется **асинхронно** и **не блокирует** торговлю:

```typescript
// После закрытия позиции (каждые 3 сделки)
calculateAutoVolume().then(volume => {
  arbitrageVolume = volume;
  // Обновляем стратегию
}).catch(error => {
  // При ошибке используем текущий объем
});
```

**Преимущества:**
- ✅ Не замедляет закрытие позиции
- ✅ Не блокирует открытие новых позиций
- ✅ При ошибке используется текущий объем (безопасность)

### Кэширование баланса

Баланс запрашивается только когда нужно:
- При запуске бота (если автообъем включен)
- Каждые 3 сделки (если автообъем включен)
- При изменении настроек

**Не запрашивается:**
- ❌ При каждом открытии позиции
- ❌ При каждом обновлении спреда
- ❌ При проверке условий закрытия

---

## 📋 Логика работы по шагам

### Сценарий 1: Запуск бота с автообъемом

```
1. Пользователь включает "Автообъем" в настройках
2. Пользователь нажимает "Запустить"
3. Бот получает баланс из API (например, 500 USDT)
4. Бот рассчитывает объем: 500 × 10 × 0.9 = 4500 USDT
5. Бот ограничивает максимальным: MIN(4500, 3500) = 3500 USDT
6. Бот устанавливает объем 3500 USDT для торговли
7. Бот начинает работать с этим объемом
```

### Сценарий 2: Торговля с автообъемом

```
Сделка 1:
- Открытие позиции: объем 3500 USDT
- Закрытие позиции: счетчик = 1
- Объем не пересчитывается

Сделка 2:
- Открытие позиции: объем 3500 USDT
- Закрытие позиции: счетчик = 2
- Объем не пересчитывается

Сделка 3:
- Открытие позиции: объем 3500 USDT
- Закрытие позиции: счетчик = 3
- ✅ Объем пересчитывается (баланс изменился, например, стал 550 USDT)
- Новый объем: 550 × 10 × 0.9 = 4950 → ограничено 3500 = 3500 USDT
- Счетчик сбрасывается в 0
```

### Сценарий 3: Торговля без автообъема

```
1. Пользователь выключает "Автообъем"
2. Объем берется из панели "Параметры ордера"
3. Объем не изменяется автоматически
4. Пользователь может вручную изменить объем в любой момент
```

---

## 🛡️ Защита от ошибок

### 1. **Ошибка получения баланса**

Если не удалось получить баланс:
- Используется текущий объем (последний успешно рассчитанный)
- Бот продолжает работать
- В логах появляется предупреждение

### 2. **Баланс равен нулю или отрицательный**

Если баланс <= 0:
- Используется текущий объем
- Бот продолжает работать
- В логах появляется предупреждение

### 3. **Ошибка расчета**

Если произошла ошибка при расчете:
- Используется текущий объем
- Бот продолжает работать
- В логах появляется ошибка

---

## 📊 Взаимодействие с другими настройками

### Авто плечо

- **Автообъем использует авто плечо** для расчета максимально возможного объема
- Если вы изменили авто плечо, объем пересчитается при следующем обновлении (каждые 3 сделки)

### Ручной объем (в панели "Параметры ордера")

- Если **Автообъем выключен**, используется объем из панели "Параметры ордера"
- Если **Автообъем включен**, объем из панели игнорируется (используется рассчитанный)

---

## 💡 Рекомендации по использованию

### Когда использовать автообъем:

✅ **Рекомендуется:**
- Если баланс часто изменяется (прибыль/убыток)
- Если хотите максимально эффективно использовать капитал
- Если не хотите вручную отслеживать баланс

### Когда НЕ использовать автообъем:

❌ **Не рекомендуется:**
- Если хотите фиксированный объем для всех сделок
- Если баланс стабилен и не изменяется
- Если нужен полный контроль над объемом каждой сделки

---

## 🔍 Логирование

Все операции автообъема логируются:

```
[AUTO-VOLUME] Calculated volume: 3500 USDT (balance: 500, leverage: 10x, percent: 90%, max: 3500)
[AUTO-VOLUME] Volume updated after 3 trades: 3500 USDT
[START] Auto volume calculated: 3500 USDT
```

Это помогает отслеживать работу функции и диагностировать проблемы.

---

## ✅ Итог

**Автообъем** - это умная функция, которая:
- 📊 Автоматически рассчитывает оптимальный объем на основе баланса
- ⚡ Не замедляет работу бота (асинхронный расчет)
- 🔄 Обновляется каждые 3 сделки для актуальности
- 🛡️ Защищена от ошибок (использует текущий объем при проблемах)
- 💰 Максимально эффективно использует доступный капитал




