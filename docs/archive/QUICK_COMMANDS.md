# 🚀 Быстрая шпаргалка команд

## 🔌 Подключение к серверу

**На вашем компьютере (PowerShell):**
```powershell
ssh root@YOUR_SERVER_IP
```

---

## 📊 Управление приложением (PM2)

```bash
pm2 status                    # Статус приложения
pm2 logs mexc-trading-bot     # Просмотр всех логов
pm2 logs mexc-trading-bot --err  # Только ошибки
pm2 restart mexc-trading-bot  # Перезапуск
pm2 stop mexc-trading-bot     # Остановка
pm2 start mexc-trading-bot    # Запуск
pm2 monit                     # Мониторинг в реальном времени
```

---

## 🔄 Обновление кода на сервере

```bash
cd /root/unified-bot
npm install                   # Установка зависимостей
npm run build                 # Сборка проекта
pm2 restart mexc-trading-bot   # Перезапуск приложения
```

---

## 📝 Редактирование файлов на сервере

```bash
nano /root/unified-bot/server.ts    # Редактировать server.ts
nano /root/unified-bot/src/...      # Редактировать другие файлы
```

**Как работать с nano:**
- Сохранить: `Ctrl+O`, затем `Enter`
- Выйти: `Ctrl+X`
- Отмена: `Ctrl+X` (если не сохраняли)

---

## 📤 Загрузка файлов на сервер

**На вашем компьютере (PowerShell):**
```powershell
# Загрузить один файл
scp unified-bot/server.ts root@YOUR_SERVER_IP:/root/unified-bot/

# Загрузить папку
scp -r unified-bot/src root@YOUR_SERVER_IP:/root/unified-bot/
```

---

## 🌐 Управление Nginx

```bash
sudo systemctl status nginx   # Статус Nginx
sudo systemctl restart nginx  # Перезапуск Nginx
sudo nginx -t                 # Проверка конфигурации
```

---

## 🔍 Проверка работы

```bash
# Проверка приложения
curl http://localhost:3002/api/health

# Проверка использования ресурсов
htop
# или
top
# (нажмите q чтобы выйти)

# Проверка диска
df -h

# Проверка памяти
free -h
```

---

## 🔥 Управление файрволом

```bash
ufw status                    # Статус файрвола
ufw allow 3002/tcp           # Разрешить порт
ufw reload                   # Перезагрузить файрвол
```

---

## 📋 Просмотр логов

```bash
# Логи приложения (PM2)
pm2 logs mexc-trading-bot

# Логи Nginx
sudo tail -f /var/log/nginx/mexc-bot-error.log
sudo tail -f /var/log/nginx/mexc-bot-access.log

# Системные логи
journalctl -xe
```

---

## 🛠️ Полезные команды

```bash
# Перезагрузка сервера
sudo reboot

# Обновление системы
apt update && apt upgrade -y

# Поиск файлов
find /root/unified-bot -name "*.ts"

# Просмотр содержимого файла
cat /root/unified-bot/package.json

# Создание бекапа
cd /root
tar -czf backup_$(date +%Y%m%d_%H%M%S).tar.gz unified-bot --exclude=node_modules --exclude=dist
```

---

## 🆘 Если что-то сломалось

```bash
# 1. Проверьте статус приложения
pm2 status

# 2. Проверьте логи ошибок
pm2 logs mexc-trading-bot --err

# 3. Перезапустите приложение
pm2 restart mexc-trading-bot

# 4. Проверьте Nginx
sudo systemctl status nginx

# 5. Проверьте файрвол
ufw status
```

---

## 📍 Пути к важным файлам

```bash
# Приложение
/root/unified-bot/

# Конфигурация Nginx
/etc/nginx/sites-available/mexc-bot

# Логи Nginx
/var/log/nginx/mexc-bot-access.log
/var/log/nginx/mexc-bot-error.log

# Конфигурация PM2
/root/unified-bot/ecosystem.config.js
```

---

## 🔄 Типичный рабочий процесс при изменении кода

1. **Подключитесь к серверу:**
   ```bash
   ssh root@YOUR_SERVER_IP
   ```

2. **Отредактируйте файл:**
   ```bash
   nano /root/unified-bot/server.ts
   ```

3. **Пересоберите:**
   ```bash
   cd /root/unified-bot
   npm run build
   ```

4. **Перезапустите:**
   ```bash
   pm2 restart mexc-trading-bot
   ```

5. **Проверьте логи:**
   ```bash
   pm2 logs mexc-trading-bot --lines 20
   ```

---

## 💡 Советы

- Всегда проверяйте логи после изменений: `pm2 logs mexc-trading-bot`
- Создавайте бекапы перед большими изменениями
- Используйте `pm2 monit` для мониторинга в реальном времени
- Если что-то не работает, сначала проверьте логи ошибок





