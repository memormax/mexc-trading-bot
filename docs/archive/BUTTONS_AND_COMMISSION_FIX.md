# Исправления кнопок и автоматической остановки при комиссии

## 1. Исправление логики кнопок

### Кнопки в панели "Статус арбитража":

#### ▶️ **Запустить** (`startArbitrageBot()`)
- **Что делает:**
  - Вызывает `POST /api/start` на сервере
  - Инициализирует компоненты (WebSocket клиенты, мониторы, стратегию)
  - Подключает WebSocket к Binance и MEXC
  - Устанавливает `isRunning = true`
  - Запускает автоматические обновления статуса и спреда

- **На сервере:**
  - Создает новые WebSocket соединения
  - Запускает мониторинг цен и стакана
  - Активирует стратегию арбитража
  - Бот начинает обрабатывать сигналы и открывать/закрывать позиции

#### ⏹️ **Остановить** (`stopArbitrageBot()`)
- **Что делает:**
  - Вызывает `POST /api/stop` на сервере
  - Отключает все WebSocket соединения
  - Очищает обработчики событий
  - Устанавливает `isRunning = false`
  - Останавливает автоматические обновления

- **На сервере:**
  - Отключает Binance и MEXC WebSocket
  - Останавливает мониторинг цен
  - Очищает стратегию арбитража
  - Бот перестает обрабатывать сигналы

#### 🔄 **Перезапустить** (`restartArbitrageBot()`)
- **Что делает:**
  - Вызывает `POST /api/restart` на сервере
  - **ИСПРАВЛЕНО:** Теперь правильно останавливает бота перед перезапуском
  - Инициализирует компоненты заново
  - Подключает WebSocket заново
  - Запускает бота снова

- **На сервере:**
  - **ИСПРАВЛЕНО:** Вызывает логику остановки напрямую (не через endpoint)
  - Очищает все компоненты
  - Создает новые компоненты
  - Подключает WebSocket заново
  - Запускает бота

#### 🔄 **Перезагрузить сервер** (`restartServer()`)
- **Что делает:**
  - Вызывает `POST /api/server/restart` на сервере
  - Останавливает все компоненты
  - Очищает все переменные
  - Готовит сервер к новым подключениям
  - **НЕ запускает бота автоматически** - нужно нажать "Запустить"

- **На сервере:**
  - Останавливает бота (если запущен)
  - Отключает все WebSocket
  - Очищает все переменные и компоненты
  - Сервер готов к новым подключениям

---

## 2. Исправление автоматической остановки при комиссии

### Проблема:
Бот не останавливался автоматически при обнаружении комиссии в последней сделке.

### Причины:
1. **Задержка обновления истории:** MEXC API может обновлять историю сделок с задержкой (1-3 секунды)
2. **Недостаточное логирование:** Не было достаточно логов для отладки
3. **Проблема с перезапуском:** После остановки бот мог быть снова запущен

### Исправления:

#### 1. Добавлено логирование:
- В `checkLastTradeForCommission()` добавлено детальное логирование:
  - Логируется обнаружение комиссии
  - Логируются все поля комиссии из последней сделки
  - Логируется результат остановки бота

- В `server.ts` добавлено логирование:
  - Логируется установка флага `stopAfterClose`
  - Логируется состояние бота и позиции
  - Логируется остановка бота после закрытия позиции

#### 2. Улучшена проверка комиссии:
- Добавлена задержка 3 секунды перед проверкой комиссии после закрытия позиции
- Это дает время MEXC обновить историю сделок

#### 3. Защита от перезапуска:
- После остановки бота через `stopAfterClose`:
  - Устанавливается `isRunning = false`
  - Отключаются все WebSocket соединения
  - Очищаются все обработчики
  - Бот не может быть автоматически запущен

### Как это работает:

1. **После закрытия позиции:**
   - Бот закрывает позицию автоматически
   - Устанавливается `lastTradeCloseTime = Date.now()`
   - Через 3 секунды вызывается `loadTradeHistory()`

2. **Проверка комиссии:**
   - `loadTradeHistory()` загружает последние сделки
   - Вызывается `checkLastTradeForCommission(orders)`
   - Проверяется последняя сделка на комиссию:
     - `totalFee` (приоритет 1)
     - `makerFee + takerFee` (приоритет 2)
     - `fee`, `commission`, `dealFee`, `dealFeeValue` (приоритет 3)

3. **Остановка бота:**
   - Если комиссия > 0:
     - Вызывается `POST /api/bot/stop-after-close`
     - Если позиция открыта: устанавливается `stopAfterClose = true`
     - Если позиции нет: бот останавливается немедленно
   - После закрытия позиции (если была открыта):
     - Проверяется флаг `stopAfterClose`
     - Если установлен: бот останавливается

### Логи для отладки:

```
[UI] 🚨 Обнаружена комиссия в последней сделке: $0.0123, останавливаем бота
[UI] Детали последней сделки: { orderId: '...', symbol: '...', fee: 0.0123, ... }
[BOT] 🛑 Запрос на остановку бота после закрытия позиции (обнаружена комиссия)
[BOT] Текущее состояние: { isRunning: true, hasPosition: true, ... }
[BOT] ✅ Позиция открыта, флаг stopAfterClose установлен
[TRADE] 🛑 Флаг stopAfterClose установлен, останавливаем бота после закрытия позиции
[TRADE] 🛑 Бот остановлен после закрытия позиции (обнаружена комиссия)
```

---

## 3. Рекомендации

1. **После остановки бота из-за комиссии:**
   - Проверьте историю сделок вручную
   - Убедитесь, что комиссия действительно была
   - Проверьте настройки API ключей (должны иметь права на чтение истории)

2. **Если бот не останавливается:**
   - Проверьте логи в консоли браузера (F12)
   - Проверьте логи на сервере (`pm2 logs mexc-trading-bot`)
   - Убедитесь, что API ключи установлены и работают
   - Проверьте, что `loadTradeHistory()` вызывается после закрытия позиции

3. **Для тестирования:**
   - Откройте позицию вручную
   - Закройте позицию вручную
   - Проверьте, что `loadTradeHistory()` вызывается
   - Проверьте, что `checkLastTradeForCommission()` вызывается
   - Если комиссия > 0, бот должен остановиться




