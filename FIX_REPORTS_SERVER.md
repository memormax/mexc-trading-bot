# 🔧 Исправление проблемы с загрузкой отчетов (команды для сервера)

## Вы уже на сервере, выполняйте эти команды:

```bash
# 1. Перейти в директорию проекта (вы уже там)
cd /root/unified-bot

# 2. Получить последние изменения из GitHub
git pull origin main

# 3. Пересобрать проект
npm run build

# 4. Перезапустить бота
pm2 restart mexc-trading-bot

# 5. Проверить логи (посмотреть что происходит с отчетами)
pm2 logs mexc-trading-bot --lines 50 | grep -i "REPORTS"
```

## Если файла отчетов еще нет:

```bash
# Создать директорию для данных (если её нет)
mkdir -p /root/unified-bot/data

# Проверить права доступа
chmod 755 /root/unified-bot/data

# Проверить, что директория создана
ls -la /root/unified-bot/data
```

## Проверка работы API:

```bash
# Проверить endpoint отчетов
curl http://localhost:3002/api/account-reports

# Или через браузер откройте:
# http://45.32.53.81:3002/api/account-reports
```

## Что должно появиться в логах:

После перезапуска в логах должно быть:
```
[REPORTS] 🔍 Путь к файлу отчетов: /root/unified-bot/data/account-reports.json
[REPORTS] ✅ Файл отчетов найден
[REPORTS] ✅ Загружено X отчетов из файла
```

Или если файла нет:
```
[REPORTS] 📁 Создаем директорию для отчетов: /root/unified-bot/data
[REPORTS] ✅ Директория создана
```

## Если все еще не работает:

```bash
# Проверить полные логи
pm2 logs mexc-trading-bot --lines 100

# Проверить статус
pm2 status

# Проверить, что файл собран
ls -la /root/unified-bot/dist/server.js
```

