# 📤 Как выложить изменения на GitHub

## Быстрая инструкция (PowerShell)

### 1. Открыть PowerShell в папке проекта

```powershell
cd D:\Cursors\uid\unified-bot
```

### 2. Проверить статус изменений

```powershell
git status
```

Покажет список измененных файлов.

### 3. Добавить все изменения

```powershell
git add .
```

Или добавить конкретные файлы:
```powershell
git add server.ts
git add QUICK_DEPLOY.md
```

### 4. Создать коммит с описанием

```powershell
git commit -m "Исправление пути к файлу отчетов на сервере"
```

Или более подробное описание:
```powershell
git commit -m "Версия 1.0.1: исправление загрузки отчетов, маскирование API ключей"
```

### 5. Отправить на GitHub

```powershell
git push origin main
```

Если это первый раз или есть проблемы:
```powershell
git push -u origin main
```

---

## Полный пример

```powershell
# 1. Перейти в папку проекта
cd D:\Cursors\uid\unified-bot

# 2. Проверить что изменилось
git status

# 3. Добавить все изменения
git add .

# 4. Создать коммит
git commit -m "Исправление загрузки отчетов на сервере"

# 5. Отправить на GitHub
git push origin main
```

---

## Если возникли ошибки

### Ошибка: "nothing to commit"
Это означает, что все изменения уже закоммичены. Просто выполните:
```powershell
git push origin main
```

### Ошибка: "Please tell me who you are"
Нужно настроить git (только первый раз):
```powershell
git config --global user.name "Ваше Имя"
git config --global user.email "ваш@email.com"
```

### Ошибка: "failed to push"
Попробуйте:
```powershell
git pull origin main
git push origin main
```

---

## Проверка после загрузки

После `git push` можно проверить на GitHub:
1. Откройте ваш репозиторий на GitHub
2. Проверьте, что последний коммит появился
3. Проверьте, что файлы обновлены

---

## Затем на сервере

После загрузки на GitHub, на сервере выполните:

```bash
cd /root/unified-bot
git pull origin main
npm run build
pm2 restart mexc-trading-bot
```









