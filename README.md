# Unified Trading Bot

Объединенный торговый бот для MEXC Futures с ручной торговлей и автоматическим арбитражем между Binance и MEXC.

## 🚀 Возможности

### Ручная торговля
- ✅ Создание ордеров (Market/Limit)
- ✅ Управление позициями (открытие/закрытие)
- ✅ Просмотр баланса и истории сделок
- ✅ Настройка параметров ордеров (плечо, объем, тип маржи)

### Автоматический арбитраж
- ✅ Автоматическое обнаружение спреда между Binance и MEXC
- ✅ Автоматическое открытие позиций при благоприятном спреде
- ✅ Умное закрытие позиций с учетом "пути отхода"
- ✅ Проверка ликвидности перед открытием позиции
- ✅ Защита от проскальзывания
- ✅ Автоматическая остановка при обнаружении комиссии

### Технические особенности
- ⚡ Оптимизированная производительность (минимальные задержки)
- 🔄 WebSocket подключения для реального времени
- 📊 Анализ стакана заявок для оценки ликвидности
- 🛡️ Защита от проскальзывания и разрывов ликвидности
- 📈 Мониторинг спреда в реальном времени

## 📋 Требования

- **Node.js**: версия 18.0.0 или выше
- **npm**: версия 9.0.0 или выше
- **MEXC Account**: аккаунт на MEXC Futures с WEB токеном

## 🔧 Установка

### Локальная установка

```bash
# Клонирование репозитория
git clone <repository-url>
cd unified-bot

# Установка зависимостей
npm install

# Сборка проекта
npm run build

# Запуск
npm start
```

### Установка на сервере

Подробное руководство по развертыванию на сервере см. в [DEPLOYMENT.md](./DEPLOYMENT.md)

## 🎯 Быстрый старт

### 1. Запуск приложения

```bash
npm start
```

Приложение будет доступно по адресу: `http://localhost:3002`

### 2. Настройка подключения

1. Откройте приложение в браузере
2. Перейдите в раздел "Подключение"
3. Получите WEB токен из браузера:
   - Войдите в MEXC Futures через браузер
   - Откройте Developer Tools (F12) → Network
   - Найдите любой запрос к `futures.mexc.com`
   - Скопируйте значение заголовка `authorization` (начинается с "WEB...")
4. Вставьте токен в поле "WEB Token" и нажмите "Подключиться"

### 3. Ручная торговля

1. Настройте параметры ордера:
   - Символ (например, UNI_USDT)
   - Плечо (1-200)
   - Объем позиции (в USDT)
   - Тип маржи (Изолированная/Кросс)
2. Выберите тип ордера (Market/Limit)
3. Нажмите "ЛОНГ" или "ШОРТ" для открытия позиции
4. Используйте "Закрыть позицию" для закрытия

### 4. Автоматический арбитраж

1. Перейдите в раздел "Настройки арбитража"
2. Настройте параметры:
   - Минимальная разница в тиках (рекомендуется: 2)
   - Размер позиции в USDT (рекомендуется: 100)
   - Максимальное проскальзывание (рекомендуется: 0.1%)
   - Символ для торговли (например, UNI_USDT)
3. Нажмите "Запустить бота"
4. Следите за статусом и спредом в реальном времени

## 📖 Документация

- **[DEPLOYMENT.md](./DEPLOYMENT.md)** - Подробное руководство по развертыванию на сервере
- **[PERFORMANCE_TIMINGS.md](./PERFORMANCE_TIMINGS.md)** - Анализ производительности и таймингов
- **[SLIPPAGE_EXPLANATION.md](./SLIPPAGE_EXPLANATION.md)** - Объяснение механизмов защиты от проскальзывания

## ⚙️ Конфигурация

### Переменные окружения

Создайте файл `.env` на основе `.env.example`:

```env
PORT=3002
HOST=0.0.0.0
NODE_ENV=production
```

### Настройки через UI

Большинство настроек можно изменить через веб-интерфейс:
- WEB Token (в разделе "Подключение")
- Параметры ордеров (в разделе "Параметры ордера")
- Настройки арбитража (в разделе "Настройки арбитража")

## 🔄 API Endpoints

### Health Check
```
GET /api/health
```

### Торговые операции
```
POST /api/orders/submit
POST /api/positions/close
GET /api/positions/open
GET /api/balance
GET /api/trades/history
```

### Управление арбитражем
```
POST /api/bot/start
POST /api/bot/stop
POST /api/bot/stop-after-close
GET /api/bot/status
GET /api/spread
```

### Управление сервером
```
POST /api/server/restart
```

## 🛠️ Разработка

### Запуск в режиме разработки

```bash
npm run dev
```

### Сборка проекта

```bash
npm run build
```

### Структура проекта

```
unified-bot/
├── src/                    # Исходный код TypeScript
│   ├── api-key-client.ts   # Клиент для API Key аутентификации
│   ├── trading-handler.ts   # Обработчик торговых операций
│   ├── monitor/             # Мониторинг цен и спреда
│   ├── trading/             # Торговые стратегии
│   └── websocket/           # WebSocket клиенты
├── ui/                      # Веб-интерфейс
│   ├── index.html
│   ├── app.js
│   ├── arbitrage-functions.js
│   └── styles.css
├── dist/                    # Скомпилированный код
├── server.ts               # Главный файл сервера
├── package.json
├── tsconfig.json
└── ecosystem.config.js      # Конфигурация PM2
```

## 🔒 Безопасность

1. **Не храните токены в коде** - используйте настройку через UI
2. **Используйте HTTPS** - при развертывании на сервере настройте SSL
3. **Ограничьте доступ** - используйте файрвол для ограничения доступа
4. **Регулярно обновляйте** - следите за обновлениями зависимостей

## 📊 Производительность

Бот оптимизирован для минимальных задержек:
- **Обнаружение сигнала**: ~1.5-6 мс
- **Обработка сигнала**: ~0.6-2.1 мс
- **"Путь отхода"**: ~0.01-0.02 мс (максимально быстро!)
- **Вход в сделку**: ~51-303 мс (зависит от сети)
- **Выход из сделки**: ~101-503 мс (зависит от сети)

Подробнее см. [PERFORMANCE_TIMINGS.md](./PERFORMANCE_TIMINGS.md)

## ⚠️ Важные замечания

1. **Токен истекает** - WEB токен MEXC имеет срок действия, при ошибках авторизации получите новый токен
2. **Реальная торговля** - бот использует реальные API и совершает реальные сделки
3. **Риски** - торговля криптовалютами связана с рисками, используйте на свой страх и риск
4. **Тестирование** - рекомендуется сначала протестировать на небольших суммах

## 🐛 Решение проблем

### Ошибка авторизации
- Проверьте, что токен не истек
- Получите новый токен из браузера
- Убедитесь, что токен начинается с "WEB..."

### WebSocket не подключается
- Проверьте интернет-соединение
- Проверьте файрвол (порты должны быть открыты)
- Проверьте логи приложения

### Позиция не открывается
- Проверьте баланс на счете
- Убедитесь, что параметры ордера корректны
- Проверьте логи на наличие ошибок

## 📝 Лицензия

MIT

## 👥 Поддержка

При возникновении проблем:
1. Проверьте логи приложения
2. Проверьте документацию
3. Убедитесь, что все требования выполнены
