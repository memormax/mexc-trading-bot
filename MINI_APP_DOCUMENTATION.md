# 📊 Документация мини-приложения для управления балансами

## 🎯 Назначение

Мини-приложение предназначено для управления балансами обработанных аккаунтов на странице отчетов. Оно работает **полностью независимо** от основного торгового бота и не мешает его работе.

## 🏗️ Архитектура

### Полная изоляция от основного бота

Мини-приложение использует:
- **Собственный модуль**: `SpotApiClient` (`src/spot-api-client.ts`)
- **Собственные API эндпоинты**: `/api/reports/*`
- **Локальные переменные**: каждый запрос создает свой экземпляр клиента
- **Разные API**: Spot API (`/api/v3/...`) вместо Futures API

Основной торговый бот использует:
- `MexcFuturesClient` для торговли
- `ApiKeyClient` для проверки комиссий
- Futures API (`/api/v1/private/...`)

**Результат**: Нет конфликтов, нет общих ресурсов, полная изоляция.

## 📋 Функциональность

### 1. Проверка баланса спотового счета

**Кнопка**: "💰 Баланс спота"

**Что делает**:
- Подключается к MEXC Spot API используя API Key/Secret из отчета
- Получает баланс спотового счета через `GET /api/v3/account`
- Отображает баланс USDT (или другую валюту, если USDT нет) рядом с кнопкой

**Технические детали**:
- Использует HMAC SHA256 подпись для аутентификации
- Параметры: `timestamp`, `signature`
- Ответ: объект с массивом `balances`

### 2. Проверка баланса фьючерсного счета

**Кнопка**: "📊 Баланс фьючей"

**Что делает**:
- Подключается к MEXC Futures API используя API Key/Secret из отчета
- Получает баланс фьючерсного счета через `GET /api/v1/private/account/asset/USDT`
- Отображает доступный баланс USDT рядом с кнопкой

**Технические детали**:
- Использует формат подписи: `HMAC SHA256(apiKey + timestamp + parameterString)`
- Заголовки: `ApiKey`, `Request-Time`, `Signature`
- Ответ: объект с `availableBalance`, `totalBalance`, `frozenBalance`

### 3. Перевод со спота на фьючерсы

**Кнопка**: "➡️ Спот → Фьючи"

**Что делает**:
1. Сначала получает текущий баланс спотового счета
2. Если баланс > 0, переводит **весь доступный баланс** на фьючерсный счет
3. Отображает статус операции ("✅ Успешно" или "❌ Ошибка") рядом с кнопкой

**Технические детали**:
- Эндпоинт: `POST /api/v3/capital/transfer`
- **Важно**: Параметры передаются в **query string**, а не в body (согласно документации MEXC)
- Формат запроса: `POST /api/v3/capital/transfer?fromAccountType=SPOT&toAccountType=FUTURES&asset=USDT&amount=X&timestamp=...&signature=...`
- Подпись: `HMAC SHA256(secretKey, sorted_query_string)`

### 4. Перевод с фьючерсов на спот

**Кнопка**: "⬅️ Фьючи → Спот"

**Что делает**:
1. Сначала получает текущий баланс фьючерсного счета
2. Если баланс > 0, переводит **весь доступный баланс** на спотовый счет
3. Отображает статус операции рядом с кнопкой

**Технические детали**:
- Аналогично переводу со спота на фьючерсы
- `fromAccountType=FUTURES`, `toAccountType=SPOT`

## 🔧 Техническая реализация

### Модуль SpotApiClient

**Файл**: `src/spot-api-client.ts`

**Класс**: `SpotApiClient`

**Методы**:
- `getSpotBalance()`: Получение баланса спота
- `getFuturesBalance()`: Получение баланса фьючерсов
- `transferFunds(fromAccountType, toAccountType, asset, amount)`: Перевод средств

**Особенности**:
- Использует нативный `https` модуль Node.js для переводов (полный контроль над запросом)
- Использует `axios` для получения балансов
- Генерирует подписи согласно документации MEXC API

### API Эндпоинты

Все эндпоинты находятся в `server.ts` (строки 4108-4221):

1. `POST /api/reports/spot-balance`
   - Body: `{ apiKey, apiSecret }`
   - Возвращает: баланс спотового счета

2. `POST /api/reports/futures-balance`
   - Body: `{ apiKey, apiSecret }`
   - Возвращает: баланс фьючерсного счета

3. `POST /api/reports/transfer-spot-to-futures`
   - Body: `{ apiKey, apiSecret, asset, amount }`
   - Переводит средства со спота на фьючерсы

4. `POST /api/reports/transfer-futures-to-spot`
   - Body: `{ apiKey, apiSecret, asset, amount }`
   - Переводит средства с фьючерсов на спот

### Frontend (UI)

**Файл**: `ui/reports.html`

**Функции JavaScript**:
- `getSpotBalance(apiKey, apiSecret, accountName, buttonElement)`
- `getFuturesBalance(apiKey, apiSecret, accountName, buttonElement)`
- `transferSpotToFutures(apiKey, apiSecret, accountName, buttonElement)`
- `transferFuturesToSpot(apiKey, apiSecret, accountName, buttonElement)`

**Особенности**:
- Балансы и статусы отображаются рядом с кнопками (не во всплывающих окнах)
- Статусы "✅ Успешно" остаются видимыми (не исчезают)
- Ошибки обрезаются до 30 символов для читаемости
- Автоматически получает весь доступный баланс для переводов

## 🔐 Безопасность и изоляция

### Почему нет конфликтов:

1. **Разные клиенты**:
   - Основной бот: `MexcFuturesClient` + `ApiKeyClient`
   - Мини-приложение: `SpotApiClient`

2. **Разные API**:
   - Основной бот: Futures API (`/api/v1/private/...`)
   - Мини-приложение: Spot API (`/api/v3/...`)

3. **Разные API ключи**:
   - Основной бот: ключи из `currentAccount` (текущий активный аккаунт)
   - Мини-приложение: ключи из отчетов (любые обработанные аккаунты)

4. **Локальные переменные**:
   - Каждый эндпоинт создает свой `SpotApiClient`
   - Нет доступа к глобальным переменным (`currentAccount`, `apiKeyClient`, `isRunning`)

5. **Разные HTTP клиенты**:
   - `SpotApiClient` создает свои `axios.create()` instances
   - Основной бот использует свои клиенты

## 📝 Использование

### Доступ к мини-приложению

1. Откройте главную страницу приложения
2. Перейдите в раздел "Мультиаккаунтинг"
3. Нажмите кнопку "Показать отчет"
4. Откроется страница отчетов в новой вкладке

### Работа с балансами

1. Найдите нужный аккаунт в таблице отчетов
2. В 4-м столбце "Управление балансом" нажмите нужную кнопку:
   - **💰 Баланс спота** - проверить баланс спотового счета
   - **📊 Баланс фьючей** - проверить баланс фьючерсного счета
   - **➡️ Спот → Фьючи** - перевести весь баланс со спота на фьючерсы
   - **⬅️ Фьючи → Спот** - перевести весь баланс с фьючерсов на спот

3. Результат отобразится рядом с кнопкой:
   - Баланс: отображается в формате "X.XX USDT"
   - Статус перевода: "✅ Успешно" (зеленый) или "❌ Ошибка: ..." (красный)

### Важные замечания

- ⚠️ Переводы происходят **всем доступным балансом** без подтверждения
- ⚠️ Убедитесь, что вы хотите перевести весь баланс перед нажатием кнопки
- ✅ Можно безопасно использовать во время работы основного торгового бота
- ✅ Не влияет на текущую торговлю
- ✅ Не изменяет состояние основного бота

## 🐛 Обработка ошибок

### Типичные ошибки:

1. **"Invalid content Type"** (700013)
   - Решено: параметры передаются в query string для POST запросов

2. **"Signature for this request is not valid"** (700002)
   - Проверьте правильность API Key и Secret
   - Убедитесь, что ключи имеют права на переводы

3. **"Balance insufficient"**
   - На счете недостаточно средств для перевода
   - Проверьте баланс перед переводом

4. **Сетевые ошибки**
   - Проверьте подключение к интернету
   - Проверьте доступность API MEXC

## 📊 Логирование

Все операции мини-приложения логируются с префиксом `[SPOT-API]` и `[REPORTS]`:

```
[SPOT-API] Request: GET /account { timestamp: '...', signature: '...' }
[SPOT-API] Response status: 200
[REPORTS] Ошибка перевода со спота на фьючерсы: ...
```

## 🔄 Обновления и изменения

### Версия 1.0.0 (2026-01-02)

- Реализована полная функциональность мини-приложения
- Исправлена проблема с "Invalid content Type" (параметры в query string)
- Добавлено отображение балансов и статусов на странице
- Обеспечена полная изоляция от основного бота

## 📚 Связанные файлы

- `src/spot-api-client.ts` - модуль SpotApiClient
- `ui/reports.html` - интерфейс страницы отчетов
- `server.ts` (строки 4108-4221) - API эндпоинты мини-приложения
- `data/account-reports.json` - хранилище отчетов

## ✅ Гарантии

**100% уверенность в отсутствии конфликтов**:
- Проведен полный анализ кода
- Подтверждена полная изоляция модулей
- Разные API, разные клиенты, разные ключи
- Нет общих глобальных переменных
- Нет общих ресурсов

**Можно безопасно использовать мини-приложение во время работы основного торгового бота!**

