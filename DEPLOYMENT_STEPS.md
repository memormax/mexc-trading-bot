# 📦 Пошаговая инструкция: Загрузка на GitHub и деплой на сервер

## ⚠️ ВАЖНО ПЕРЕД НАЧАЛОМ
- На сервере сейчас работает **старая версия** без мультиаккаунтинга
- Мы заменим её на **новую версию** с мультиаккаунтингом
- **Рекомендуется**: Остановить бота на сервере перед деплоем (опционально, но безопаснее)

---

## 📤 ЧАСТЬ 1: Загрузка на GitHub

### Шаг 1: Проверка текущего состояния
Откройте PowerShell в папке проекта и выполните:
```powershell
cd D:\Cursors\uid\unified-bot
git status
```

Вы должны увидеть список измененных и новых файлов.

### Шаг 2: Добавление всех изменений
```powershell
git add .
```

Эта команда добавит все изменения в staging area.

### Шаг 3: Проверка что будет закоммичено
```powershell
git status
```

Убедитесь, что все нужные файлы добавлены (backup/ не должен быть в списке, это нормально).

### Шаг 4: Создание коммита
```powershell
git commit -m "Добавлен мультиаккаунтинг и оптимизации: проверка условий после обновления баланса"
```

### Шаг 5: Отправка на GitHub
```powershell
git push origin main
```

**Если запросит пароль:**
- Используйте ваш **Personal Access Token** (не обычный пароль)
- Токен должен начинаться с `github_pat_...`

### Шаг 6: Проверка на GitHub
Откройте в браузере: https://github.com/memormax/mexc-trading-bot
- Убедитесь, что последний коммит появился
- Проверьте, что все файлы загружены

---

## 🚀 ЧАСТЬ 2: Деплой на сервер

### ⚠️ ВАЖНО: Подготовка сервера

**Вариант А (Рекомендуется): Остановить бота перед деплоем**
```bash
# На сервере выполните:
pm2 stop mexc-trading-bot
```

**Вариант Б: Деплой без остановки (бот продолжит работать со старой версией до перезапуска)**

---

### Шаг 1: Подключение к серверу
Откройте PowerShell и выполните:
```powershell
ssh root@45.32.53.81
```
(Введите пароль от сервера)

---

### Шаг 2: Переход в папку проекта
```bash
cd /root/unified-bot
```

---

### Шаг 3: Проверка текущей версии (опционально)
```bash
git log --oneline -5
```
Посмотрите последние коммиты, чтобы убедиться какая версия сейчас на сервере.

---

### Шаг 4: Остановка бота (если не остановили ранее)
```bash
pm2 stop mexc-trading-bot
```

---

### Шаг 5: Сохранение текущей версии (бекап на сервере)
```bash
# Создаем бекап текущей версии на сервере
cp -r /root/unified-bot /root/unified-bot_backup_$(date +%Y%m%d_%H%M%S)
```

---

### Шаг 6: Получение новой версии с GitHub
```bash
# Переходим в папку проекта
cd /root/unified-bot

# Получаем последние изменения
git pull origin main
```

**Если запросит пароль:**
- Используйте ваш **Personal Access Token** (не обычный пароль)

---

### Шаг 7: Установка зависимостей (если нужно)
```bash
npm install
```

---

### Шаг 8: Компиляция TypeScript
```bash
npm run build
```

Проверьте, что компиляция прошла без ошибок.

---

### Шаг 9: Проверка .env файла
```bash
cat .env
```

Убедитесь, что файл `.env` существует и содержит правильные настройки:
```
PORT=3002
HOST=0.0.0.0
NODE_ENV=production
```

Если файла нет, создайте его:
```bash
cat > .env << 'EOF'
PORT=3002
HOST=0.0.0.0
NODE_ENV=production
EOF
```

---

### Шаг 10: Перезапуск бота
```bash
pm2 restart mexc-trading-bot
```

Или если бот не был запущен:
```bash
pm2 start ecosystem.config.js
```

---

### Шаг 11: Проверка статуса
```bash
pm2 status
pm2 logs mexc-trading-bot --lines 50
```

Проверьте:
- ✅ Статус: `online`
- ✅ Нет ошибок в логах
- ✅ Бот запустился успешно

---

### Шаг 12: Проверка через браузер
Откройте в браузере: http://45.32.53.81/

Проверьте:
- ✅ Страница загружается
- ✅ Интерфейс работает
- ✅ Можно войти в систему

---

## 🔄 Если что-то пошло не так (Откат)

### Вариант 1: Откат через Git
```bash
cd /root/unified-bot
git log --oneline -5  # Посмотреть историю коммитов
git reset --hard <commit-hash>  # Вернуться к предыдущему коммиту
npm run build
pm2 restart mexc-trading-bot
```

### Вариант 2: Восстановление из бекапа
```bash
# Найти бекап
ls -la /root/unified-bot_backup_*

# Восстановить (замените дату на вашу)
rm -rf /root/unified-bot
cp -r /root/unified-bot_backup_20251228_110000 /root/unified-bot
cd /root/unified-bot
npm install
npm run build
pm2 restart mexc-trading-bot
```

---

## ✅ Чеклист после деплоя

- [ ] Бот запущен (`pm2 status` показывает `online`)
- [ ] Нет ошибок в логах (`pm2 logs`)
- [ ] Веб-интерфейс доступен (http://45.32.53.81/)
- [ ] Можно войти в систему
- [ ] Мультиаккаунтинг работает (проверить в интерфейсе)
- [ ] Настройки сохраняются
- [ ] Бот может торговать (протестировать запуск)

---

## 📝 Полезные команды для сервера

```bash
# Просмотр логов
pm2 logs mexc-trading-bot

# Перезапуск бота
pm2 restart mexc-trading-bot

# Остановка бота
pm2 stop mexc-trading-bot

# Статус бота
pm2 status

# Просмотр последних 100 строк логов
pm2 logs mexc-trading-bot --lines 100

# Проверка порта
curl http://localhost:3002/api/health
```

---

## 🆘 Если возникли проблемы

1. **Проверьте логи**: `pm2 logs mexc-trading-bot --lines 100`
2. **Проверьте статус**: `pm2 status`
3. **Проверьте компиляцию**: `npm run build`
4. **Проверьте .env файл**: `cat .env`
5. **Проверьте права доступа**: `ls -la /root/unified-bot`

Если ничего не помогает - откатитесь к предыдущей версии (см. раздел "Откат").













